
## 11:28 — Stop auto-stamping every new business with a fulfilment pickup location

Traced why Polyform's order `PLF254-96337-AB611` never produced a pickup job: `Business.fulfillment_service_enabled` defaults to True, so the `create_fulfillment_store_on_service_enable` post_save receiver fired on every new business and created an `is_fulfilment_center=True` PickupLocation pointing at the default warehouse — even for clients with no SellerWarehouseLink. That stale FC flag makes `create_pickup_task_if_needed` refuse with `fulfilment_center`, so those clients silently got no first-mile pickup jobs. 50 of 53 businesses carried such a row while only 10 had a real warehouse link.

Removed that receiver (left an explanatory note in its place) and deleted the two bulk creators that would reintroduce it (`create_fulfillment_stores` management command + the matching `.claude/scripts` copy). Fulfilment pickup rows are now created in exactly one place — `warehouse.signals.seller_warehouse_link_post_save`, which fires when staff link a business to a warehouse in the warehouse dashboard. `fulfillment_service_enabled` keeps its `default=True` per instruction, so no migration. Data pass: unticked `is_fulfilment_center` (and cleared the warehouse FK) on the 34 stale rows that had zero order/task references; the 6 with live orders were left alone. Rewrote `tests_fulfillment_consolidation` — the old tests asserted the auto-create — now 5/5 pass.

**Files:** `business/signals.py`, `business/tests_fulfillment_consolidation.py`, `business/management/commands/create_fulfillment_stores.py` (deleted), `.claude/scripts/create_fulfillment_stores_script.py` (deleted)

## 11:52 — First-mile pickup now also opens on the "ready to pickup" status move
Traced why Polyform order PLF254-96337-AB611 never got a pickup task: its only pickup address (POLYFORM, id 130) carries a stale `is_fulfilment_center=True` stamp from the old auto-create signal and was flipped `inactive` when staff turned fulfilment service off, so the pickup gate refuses with `fulfilment_center`. Separately, changing an order to `ready_to_pickup` was never a pickup trigger at all — only order-create, a pickup location first arriving, and the staff repair hatch were. Added a third hook in the order post_save that re-runs `create_pickup_task_if_needed` when the order transitions *into* `ready_to_pickup`, so a leg opens once staff repair the client's address config. The service is idempotent, so it can never open a second leg. Four new tests cover the repaired-config case, the still-refused case, the no-duplicate case, and that only the transition fires (not every later save). The Polyform data itself is still unfixed — the DB write was blocked by the permission classifier and staff will do it through the seller page.

**Files:** `orders/signals.py`, `delivery/tests_pickup.py`

## 12:55 — Turning fulfilment service off no longer strands a client without a pickup address
The seller-edit page's fulfilment toggle deactivated every `is_fulfilment_center=True` pickup row on the way off, which was the third leg of the Polyform bug: the client's own shop address wore a stale FC stamp from the removed auto-create signal, so switching the service off killed its only collectable address and first-mile pickup refused forever on `fulfilment_center`. Added `_retire_fulfilment_pickup_locations` in `workforce/views.py`, which now tells the two row types apart — a row backed by an active `SellerWarehouseLink` is a real warehouse and gets deactivated (keeping its flag, so drivers are never sent to collect goods already at the hub), while an unbacked row is just the shop address and gets its stale flag cleared so it stays usable. The same branch also downgrades `fulfillment_service_status` from `active` to `none`, since much of the codebase reads status rather than enabled and a disabled client was otherwise still sitting in every fulfilment queue and report.

Five new tests in `FulfilmentToggleOffTestCase` cover both row types, the status downgrade, a never-onboarded client keeping its status, and that pickup actually works again after the toggle. Full file 10/10 passing; the surrounding suites were checked against a stash of the change and their failures are pre-existing at HEAD.

**Files:** `workforce/views.py`, `business/tests_fulfillment_consolidation.py`

## 13:05 — Pickup pool hero/console: finished the interrupted pass and fixed the button CSS
The previous session dropped mid-way through verifying the pickup-pool restructure (two-row hero with the tallies + Create-pickup + notes button in the aside and the filter rail underneath, date-window filters, "Create pickup" repair hatch, per-row location relocate dropdown, on-demand Timeline strip). The template, view, URLs and CSS were all already written; what was left was the interaction check.

Found and fixed one real defect in `workforce.css`: `.ppl__mkcard .btn.ppl__btn` had been pasted into *every* selector group in the ppl button block instead of the matching state/variant, so it inherited the hover fill, `opacity: 0.65`, the quiet skin, the quiet-disabled `opacity: 0.4`, the warn and danger borders and both disabled rules at once — every button inside the Create-pickup panel rendered as a washed-out white button with a red border. Each of the 12 wrong lines now carries the right state (`:hover`, `--quiet`, `--quiet:disabled`, `--warn`, `--danger`, `--warn:disabled`, `--danger:disabled`, `.ppl__code`).

Verified rather than assumed: template parses, `manage.py check` clean, the page renders 200 across the default/all/30-day/custom-range/mode/search filters with the row counts changing as expected, both new POST endpoints return clean JSON refusals for bad input (not 500s), and the timeline endpoint returns the shared `.pkl` card (404 on a missing id). The default 7-day window showing zero rows is correct, not a bug — every pickup in the DB is `handed_off`/`cancelled`/`dropped`, none active, so `hidden_active` is legitimately 0. Bumped `workforce.css` to `?v=20260826g`, collected static, HUP'd gunicorn, and confirmed the fixed selector in the CDN-served file.

**Files:** `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 13:18 — "Collect from" dropdown keeps a fixed width
The Create-pickup panel's location select carried Bootstrap's `w-auto`, so its width tracked its contents: a sliver before an order was looked up (the select starts with no options at all), then however wide the longest `title — locality` label happened to be once it filled, shoving the "Create pickup" button along the row. Swapped `w-auto` for a `ppl__mkloc` BEM class pinned at `width: 20rem` with `text-overflow: ellipsis`, so the field holds its place empty or full and a long address truncates instead of reflowing the panel. Under 768px it drops to `width: 100%`, since 20rem is wider than a phone once the panel padding is off. Verified the rendered markup and the served CSS.

**Files:** `workforce/templates/workforce/pickup_pool_status.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 13:34 — From/To boxes only appear under the "Custom range…" preset
The pickup pool's date group always showed all five controls — the quick preset plus From, To and their two labels — even though the boxes do nothing under "Last 7 days" or "All time". They now render with `hidden` under every preset except Custom, and `ppPresetRange` reveals them when staff pick "Custom range…" (before focusing, since a hidden input cannot take focus) and re-hides them when they pick a preset back.

Kept them as direct children of `.ppl__fdates` rather than wrapping them in a container, because the mobile block reaches them with `>` child selectors. Needed an explicit `.ppl__fdates [data-pp-range][hidden] { display: none; }` rule: Bootstrap's `.form-control` declares `display: block`, and an author rule outranks the UA sheet's `[hidden]` regardless of specificity, so without it the date inputs would have stayed on screen. Verified across four presets that the server renders the right hidden state, and that a hidden box submits empty so the view's "custom wins" branch can't fire on a stale value.

**Files:** `workforce/templates/workforce/pickup_pool_status.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 14:58 — "To Publish" list keyed off status, and the green "Published" badge made reachable
Investigating why Polyform order PLF254-96337-AB611 was missing from `/workforce/orders/to_publish/` turned up two real defects. The page filtered on `task_created=False`, a one-way latch set when a delivery task is created and never reset — so it listed nothing at all in production, and an order sent back to "Hold for Review" could never reappear on it. It now filters on `order_status in (to_review, ready_to_pickup)`, and the sidebar counter mirrors it. (The order itself was fine: staff had bulk-set four Polyform orders back to Hold for Review at 12:02.)

Second, the only valid status value is `publish` (display "Published"), but templates and view whitelists tested the string `published`, which is not a choice at all. Worst case was `update_order_status`, whose hand-written whitelist accepted the invalid `published` while *omitting* the real `publish` — and `order_status` is a plain CharField, so that whitelist was the only guard. Both whitelists now derive from `ORDER_STATUS_BY_CLIENT`, which also drops the never-valid `processing`/`to_publish`/`reported` (the "Reported" bulk button went with them). Four templates hand-rolled the same status→badge chain and had drifted apart; they now share `order_status_class` / `order_status_badge` filters plus `get_order_status_display`, so the label can't diverge from the model again. The order detail panel had no `publish` branch at all and was printing the raw key in a grey chip — it shows a green "Published" now.

Also: bulk and single status changes now stamp `_status_changed_by`, so `OrderStatusHistory` names the staffer instead of leaving `changed_by` null (the actor previously had to be recovered by cross-joining `OrderVerificationLog` on timestamp), and a `cStatus` filter value that isn't a real choice is ignored rather than silently returning zero rows. Exercised the write path end to end: publish, invalid-value rejection, badge rendering, and the round trip back onto the list. Suite is 254 tests / 8 failures, all pre-existing and unrelated (2 stale CSV content-type asserts, 3 warehouse inventory, and 3 others); one test that asserted the old bug by posting `processing` was corrected.

**Files:** `workforce/views.py`, `workforce/context_processors.py`, `core/templatetags/custom_filters.py`, `workforce/templates/workforce/parts/lists/orders_list_view.html`, `workforce/templates/workforce/parts/_order_detail_content.html`, `orders/templates/orders/parts/order_row.html`, `orders/templates/orders/parts/order_list_review.html`, `orders/templates/orders/parts/order_list_view.html`, `webpages/static/webpages/js/dashboard-utils.js`, `templates/includes/scripts-dashboard.html`, `workforce/tests_views.py`
