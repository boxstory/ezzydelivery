
## 06:08 — AI Config page: tabs, per-section save, live tests, master toggles
Rebuilt /workforce/auto-triggers/ai-config/ for more control (all four asks). (1) Tabs: Overview / Chat AI / WhatsApp AI / API Access / Budgets & Limits instead of one long two-column form. (2) Per-section save: each tab is its own form that AJAX-POSTs only its fields (section=…) so saving one area never touches another — verified a flags-only save preserved the API token; view returns JSON for XHR, still HUPs gunicorn, keeps redirect fallback. (3) Live tests: new wf_ai_config_test endpoint — Chat/WhatsApp "Test reply" runs a real sample prompt through that purpose's chat service (shows model + latency + reply), "Test API" verifies the conversation-API token+agent (no cost). (4) Prominent master switches (AI Agent, WhatsApp AI) in the hero that instant-save on toggle. Provider/key markup moved to context-driven loops (providers, api_keys). New aic__ CSS for tabs/switch/form-foot/test-result/toast; cache-buster v7.

**Files:** `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/ai_config.html`, `workforce/static/workforce/css/ai-config.css`

## 08:11 — AI Config: provider list shows availability (API key configured)
The Chat/WhatsApp provider lists previously showed all 6 providers with no indication of which are usable. Each provider row now carries a Ready (green, key configured) or No key (grey, dimmed row) badge, computed in the view from whether that provider's API key env is set; a "N/6 have a key · add keys" hint under the Provider heading jumps to the API Access tab. Live per-provider connectivity is still available via the existing "Test reply" button. CSS v8.

**Files:** `workforce/views.py`, `workforce/templates/workforce/ai_config.html`, `workforce/static/workforce/css/ai-config.css`

## 14:32 — Removed card hover lift on AI Agent Config page
The cards on /workforce/auto-triggers/ai-config/ were floating up on hover because of the global brandkit `.card:hover { transform: translateY(-0.125rem) }` rule. Added a scoped override in ai-config.css that cancels the transform and the transition inside `.aic` only, so other pages keep the effect. Bumped the CSS cache-buster to v=9, collected static and reloaded gunicorn.

**Files:** `workforce/static/workforce/css/ai-config.css`, `workforce/templates/workforce/ai_config.html`

## 14:41 — Fixed unreadable active tab on AI Agent Config
The active tab ("WhatsApp AI") rendered as dark #333 fill with near-black #1f1f1f text because brandkit-overrides.css sets `.nav-tabs .nav-link.active` with `!important`, which beat the page's own tab styling. Re-declared the `.aic__tabs` active/hover rules with `!important` so the tabs go back to the intended look: transparent background, navy label, yellow underline. Cache-buster to v=10.

**Files:** `workforce/static/workforce/css/ai-config.css`, `workforce/templates/workforce/ai_config.html`

## 14:55 — AI Config tabs: readable, clearly distinct hover/active states
Verified the tab rendering in headless Chromium against the real CSS chain rather than guessing. The earlier `!important` override did take effect, but the resulting states were too faint to read — active and hover looked the same as idle. Rewrote the `.aic__tabs` rules so all three states are explicit: idle grey text, hover navy text on a light grey tint, active navy bold on a cream tint with a 3px yellow underline; the base rule now also neutralises brandkit's dark `.nav-tabs .nav-link` fill. Cache-buster to v=11.

**Files:** `workforce/static/workforce/css/ai-config.css`, `workforce/templates/workforce/ai_config.html`

## 15:04 — AI Config tabs switched to filled segmented buttons
The underline treatment was still too quiet to read at a glance, so the tabs are now segmented buttons: idle is a light grey chip with dark grey label, hover darkens the chip and turns the label navy, and the active tab is a solid navy chip with white bold text and a yellow inset bar along its bottom edge. Confirmed the served stylesheet on production carries the new rules and re-rendered the strip in headless Chromium to check contrast. Cache-buster to v=12.

**Files:** `workforce/static/workforce/css/ai-config.css`, `workforce/templates/workforce/ai_config.html`

## 08:50 — Add 9Router (self-hosted gateway) as an AI provider + availability-by-key
Added 9Router — a self-hosted OpenAI-compatible LLM gateway (models like oc/deepseek-v4-flash-free) — as a selectable Chat/WhatsApp provider. OpenAICompatService gained a 'router' provider whose endpoint comes from ROUTER_BASE_URL (settings) instead of a hardcoded host; get_chat_service routes 'router' to it. AI Config API Access tab now has a 9Router base-URL + API-key field; its model dropdown live-fetches from {ROUTER_BASE_URL}/models. Also made the provider rows show availability (Ready when key/base configured, No key + dimmed otherwise). Saved the provided ROUTER_API_KEY; ROUTER_BASE_URL still blank because the gateway (docker.yellowkey.qa, a separate VPS) sits behind an nginx basic-auth wall (all paths 401) so this server can't reach its /v1 API yet — needs a reachable base URL. Fixed a decouple empty-value KeyError (try/except pattern for ROUTER_BASE_URL).

**Files:** `ai_agent/services/unified_service.py`, `ezzydelivery/settings.py`, `.env`, `workforce/views.py`, `workforce/templates/workforce/ai_config.html`, `workforce/static/workforce/css/ai-config.css`

## 09:03 — Route all app AI through the 9Router gateway (AI_BASE_URL/AI_API_KEY)
Connected this VPS to the shared 9Router AI gateway (OpenAI-compatible) at https://docker.yellowkey.qa/v1. Stored AI_BASE_URL + AI_API_KEY in .env (secret, not committed); renamed the earlier ROUTER_* wiring to those names across settings/service/config UI. The router provider default model is now "free" (gateway routing combo). Set AI_CHAT_PROVIDER/AI_WA_PROVIDER=router + models=free so dashboard chat, order extraction, address lookup and WhatsApp replies all route through the gateway. curl verified /v1/models + /v1/chat/completions = 200; the app AI client (get_chat_service) returns clean replies 3/3. Fixed a JSONDecodeError "Extra data" from the free routing by (a) sending stream:false and (b) a _loads_lenient parser that raw_decodes the first JSON object; also fall back to message.reasoning when content is null. Server public IP for gateway allow-listing: 2a02:4780:5e:e726::1 (IPv6).

**Files:** `ai_agent/services/unified_service.py`, `ezzydelivery/settings.py`, `.env`, `workforce/views.py`

## 09:20 — Verified 9Router routing end-to-end (incl. tool-calling)
Confirmed the whole app works through the gateway: plain agent replies 3/3, tool-calling agent invokes lookup_zone/parse_address/search_orders correctly and executes them, the aiagent conversation API returns clean replies, site HTTP 200. Investigated an apparent tool failure — it was a bad test query ("how many orders delivered today" has no matching staff tool, so _select_tools correctly returned None); real tool-mapped queries call tools fine. Also confirmed raw gateway forwards tools and returns tool_calls (free -> north-mini-code-free). No code change needed beyond the earlier stream:false + lenient-JSON fixes.

**Files:** (verification only)

## 09:27 — AI Config console redesign (all five tabs)
Rebuilt the /workforce/auto-triggers/ai-config/ page around a "credential ledger" idea: navy hero (the master toggles were previously white-on-white and invisible), a one-line brief strip on every tab explaining what it governs, and mono type for all data (keys, endpoints, model ids, amounts). API Access is the centrepiece — the seven provider keys are now collapsed ledger rows that expand in place instead of seven always-open password fields, and every provider stays reachable (the old JS hid any provider not currently selected, so you could not add a key for one you had not already picked). The Conversation API card gained a copyable endpoint plate, a ready-to-paste curl example, copy buttons, and the history/close/health routes. Overview replaced the four floating stat cards with a single instrument strip plus a "where the agent runs" routing table; Chat/WhatsApp tabs got a primary→fallback routing summary and now flag only the exception ("No key") instead of five green READY tags; Budgets shows live spend next to each cap. Provider pastel chips, the yellow Save buttons and the last inline style were all removed.

**Files:** `workforce/templates/workforce/ai_config.html`, `workforce/static/workforce/css/ai-config.css`, `workforce/views.py`

## 10:21 — Verify AI parse-location + client dashboard chat on gateway; fix fallback
Tested the two surfaces the user asked about through the 9Router gateway. AI parse-location (ParseAddressTool._ai_search_zone → get_chat_service) works — infers zone/coords for free-text Qatar addresses (e.g. Al Sadd near City Center → Zone 38 + coords; QNAS-exact for structured). Client/business dashboard chat works — get_business_dashboard / get_business_cod_summary tools invoked and answered ("COD outstanding is 0 QAR"). Uncovered gateway free-routing flakiness on multi-step tool flows: routes to rate-limited free upstreams (Groq 429) and reasoning models that exhaust max_tokens on thinking → empty final answer. Fixed the FallbackService: it crashed on a ClaudeService fallback (no .provider attr) — added _pname() safe accessor; now also falls back on a successful-but-empty reply (no content + no tool_calls), not just rate-limit errors. Fallback provider set to zhipu (GLM-4.7-flash) — the only provider with a valid working key (Anthropic key 401/invalid, OpenAI 429/quota, Groq unreliable). Result: dashboard chat returns real answers reliably.

**Files:** `ai_agent/services/unified_service.py`, `.env`

## 12:17 — Pickup Pool hero collapsed by the status dropdown + branded pickers
The navy hero on /workforce/pickups/ was collapsing into a ~200px band with the title and tallies squashed into a corner. Cause: the global staff-dashboard Select2 enhancer in `main_dashboard_scripts.html` grabs every select inside a GET form and applies `width: '100%'`, so the two compact hero filters rendered as full-width white boxes stacked on three rows. Marked them `data-no-select2` and enhanced them locally instead with `width: 'auto'`, so the hero is back to a single 60px row. Then, per the follow-up ask, gave the pickers a proper Brand Kit skin: `ppl__pick` (navy translucent trigger, white 600 text, yellow chevron that flips on open, yellow focus border) and `ppl__pick-menu` (white hairline panel, radius-sm, ink options at 0.78rem, navy highlight, current selection on grey-100 with a yellow left rule). All selectors carry `body .select2-container--default` because base-forms.css paints Select2 controls white at that same weight. Verified live in headless Chrome: hero 59.7px, picking an option still submits the filter (change-event bridge), mobile at 420px stacks cleanly, no console errors.

**Files:** `workforce/templates/workforce/pickup_pool_status.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 12:31 — Pickup Pool picker: no border change on hover
Hover on the hero pickers now only lifts the fill (0.08 → 0.12 white) — the border stays at the resting `rgba(255,255,255,.25)` and never shifts. The yellow border is reserved for when the menu is actually open; previously Select2 left `select2-container--focus` on the control after the menu closed, so a stale yellow border sat on the field and showed up on every later hover. Keyboard arrivals keep a yellow ring via `:focus-visible`, which the mouse path does not trigger.

**Files:** `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 15:28 — Pickup Pool: return-to-pool (accepted → pending) + multi-select rows
Staff can now take a claimed pickup back off a driver: a "Back to pool" action on every accepted / in-progress / arrived row sets the task to Pending, clears the driver and accepted_at, writes the transition to the order timeline, notifies the driver it was removed, and re-notifies the client's assigned fleet so the task actually gets re-claimed instead of sitting silent. Collected and later statuses are refused — the goods are already with the driver at that point. Added a multi-select layer to the table on the same pass: a checkbox gutter with select-all and shift-click ranges, a bulk bar above the table that shows the count and offers "assign all to driver" (reuses the fleet driver search) and "Back to pool", plus Clear. Both endpoints now take `pickup_ids[]` as well as the old single `pickup_id`, apply per-row, and report exactly which rows were skipped and why. Verified against live data in a rolled-back transaction: single unassign, bulk assign of 3, and a mixed selection that correctly refused a handed-off task.

**Files:** `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/pickup_pool_status.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 12:35 — COD payout to client: net/gross agreement, frozen fees, payout history, upstream fences
Audited the whole client COD payout leg (Leg 3) and closed the gaps the 07-28 net-invoice rework left behind. The payout transaction correctly holds the NET that reaches the bank, but three screens still read it as if it were gross: the staff "COD to settle" figure subtracted a net number from gross legs so it could never close to zero, and the seller's finance dashboard showed the withheld charges a second time as money still owed. All three now go through new `WalletService.payout_figures()` / `payout_totals()`, which add the invoice lines back, and the seller's charge list excludes anything already recovered at payout (`deduction_line__isnull=True`). The `business_cod_settled` auto-flow was also still quoting the gross to the business — it now sends the net actually transferred plus the gross, the deductions and the invoice code.

Charge resolution (`verified_delivery_charge` else `dl_price`) was written out by hand in five places that had to stay in step; it now lives once in `delivery/charges.py` as `billable_charge()` / `BILLABLE_CHARGE` / `charge_paid()`, with `DeliveryTask.billable_charge` for templates. That also fixed a live bug on the Client Charges console, which used `|default:` instead of `|default_if_none:` — a charge staff verified at 0.00 displayed and re-submitted `dl_price` instead of zero.

The invoice used to recompute its per-order fee column live at render while the deduction line was frozen, so editing a verified charge after a payout silently made an issued invoice contradict the money that moved. New `DeliveryTask.settled_delivery_charge` (migration delivery/0041) freezes the fee each task paid at payout time, the reversal clears it, and the Client Charges console now refuses edits on any task already paid to the business rather than only on published ones. The payout action also stopped lying about failure: a per-business exception was swallowed and the response still returned success with nothing settled, so staff read a failed payout as a completed one — it now reports settled/failed/skipped per account and returns 400 when nothing moved.

Added a staff Payout History page (`/workforce/fleet/cod-business-settlement/history/`, sidebar desktop + mobile) — a payout was previously reachable only in the seconds after creation and voiding one meant typing its CODCS code from memory — and a seller-scoped read-only copy of the invoice at `/business/finance/cod-payout/<code>/`, linked from every settlement row on the COD statement, which now states COD released → charges → paid to you instead of one unexplained figure.

Finally fenced the two upstream paths that could feed wrong money into the payout queue: `bulk_settle_transactions` had no `cod_settled` check, leaving 627 already-deposited COD rows (QAR 194,908) selectable — settling them would mint a phantom deposit and overwrite `cod_submission_txn` — and it computed its totals outside the atomic block; it now excludes deposited rows and locks before reading (on a flat id filter, since FOR UPDATE cannot span the nullable task join, re-validating after the lock). `recalc_wallet --apply` force-set `cod_settled=True` from a ledger-derived figure, which would have made 31 driver-held tasks (QAR 11,575) instantly payable; that now needs an explicit `--settle-tasks` and otherwise just reports the task ids.

Verified end-to-end on production data inside a rolled-back transaction: 570 gross − 60 delivery − 25 fulfilment − 10 cargo = 475 booked exactly, three deduction lines, fee frozen at 20.00 per task and summing to the delivery line, invoice/history/payout desk/client charges/finance all 200, a charge edit after payout refused (skipped_paid 1, figure unchanged), reversal returning exactly 475 and clearing the freeze, seller pages 200 and another business's invoice 404. All driver balances byte-identical before and after. 17 new tests; `manage.py test fleet` runs 189, OK.

**Files:** `delivery/charges.py`, `delivery/models.py`, `delivery/migrations/0041_deliverytask_settled_delivery_charge.py`, `fleet/wallet_service.py`, `fleet/tests.py`, `fleet/management/commands/recalc_wallet.py`, `workforce/views.py`, `workforce/urls.py`, `business/views.py`, `business/urls.py`, `workforce/templates/workforce/cod_business_payout_history.html`, `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/templates/workforce/cod_business_settlement_report.html`, `workforce/templates/workforce/client_charge_verification.html`, `workforce/templates/workforce/parts/sidebar/finance.html`, `workforce/templates/workforce/parts/dashboard_sidebar_workforce_mob.html`, `business/templates/business/parts/business_cod_statement.html`, `business/templates/business/parts/business_finance_dashboard.html`, `workforce/static/workforce/css/cod_business_settlement.css`

## 15:37 — Pickup Automation: rule notes moved out of the hero behind a help button
The "each new order creates a pickup job…" line was low-contrast grey text inside the navy hero. Moved it out into a proper notes card (`pau__brief`) that sits between the hero and the table, at full ink contrast, and extended it with one-line definitions of the two pool modes (Assigned fleet vs Public pool) since that is the column it explains. The card is hidden on load — a round "?" button at the end of the hero toggles it and turns yellow while open (aria-expanded + a screen-reader Show/Hide label). Also pinned the hero search field: with the copy gone it stretched across the freed space and base-forms.css was painting it solid white, so it now holds 14rem with the translucent navy skin the pool page uses. Verified in headless Chrome — hero back to 59.7px, closed on load, opens/closes on click, no console errors.

**Files:** `workforce/templates/workforce/pickup_automation_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 15:40 — Pickup Automation notes card: full page guide
Expanded the collapsed notes card from two lines into a real guide to the page. It now opens with what a first-mile pickup actually is and how a row's rule reaches a job (copied onto the pickup raised when an order is created), then covers each control — Auto pickup, Pool mode, After collection, the Manage fleet panel (add / pause / remove, only active drivers counted), and the per-row Save with its audit log entry. Both rule vocabularies are spelled out: the two pool modes including the "empty assigned fleet means nobody is notified" trap, and all three post-collection dispositions. A "Worth knowing" block closes it with the things staff get wrong — changes only affect orders created from then on, the five conditions where no pickup job is raised at all, order cancellation cascading to the pickup, and the pickup leg carrying no COD and no driver earnings. Laid out in two reading columns on desktop, stacked on mobile.

**Files:** `workforce/templates/workforce/pickup_automation_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 13:05 — Seller Transactions: drop column shows the real destination zone
The "From / To Location" column printed `order.customer_address` for the drop — free text that arrives with the order ("lusil", "om ebriya", "aldafina", sometimes blank) — while the order's actual destination sits in `dl_zone`/`dl_street`/`dl_building` and was never shown. The two often disagree: an order reading "lusil" is on zone 70, which is Semaisma, not Lusail. Against the tidy pickup line ("WH: EzzyDelivery FC- Sudan · Zone N") the drop read as noise.

The drop now leads with the official destination — "Zone 66 · Al Gassar" — with street and building underneath, and the text that came with the order kept below in quotes and muted. Keeping it visible rather than dropping it means a mismatch between what was typed and where the order is actually going shows up on the row instead of being hidden. Orders with no zone say "No zone" instead of rendering an empty cell.

Zone names come from one query over the 96 active `ZoneName` rows built into a map in the view, read in the template through a new `get_zone_name` filter alongside the existing `get_zone_group` in `delivery/templatetags/zone_tags.py` — never a query per row.

**Files:** `workforce/views.py`, `workforce/templates/workforce/seller_transactions.html`, `delivery/templatetags/zone_tags.py`, `workforce/static/workforce/css/fleet_transactions.css`, `templates/wf_dashboard_base.html`

## 13:50 — Pickup→drop distance stored on the order and shown on the ledger + order detail
Added a straight-line pickup→drop distance, computed once and stored on the order so every screen quotes the same number instead of recalculating. New `Order.route_distance_km` + `route_distance_exact` (migration orders/0056), kept current by a `_refresh_route_distance` helper on the order post_save signal — written with a queryset update so it also lands when the caller saved with `update_fields` (a coords-only save would otherwise leave the distance stale) and cannot recurse back into the receiver.

`delivery/geo.py` holds the resolver: the drop uses the order's own coordinates, falling back to the drop zone's centre; the pickup uses the pickup location's coordinates, falling back to its zone centre. Any fallback sets `route_distance_exact=False` so the figure can be marked as an estimate rather than passed off as a measurement. It reuses `TaskStatusPoint.haversine_km` rather than adding a fifth copy of the formula to the repo.

A bounds guard was needed: one live order carried `lat=2.0` — a truncated 25.x — which produced a 2,588 km "delivery". Coordinates outside Qatar's bounding box are now discarded in favour of the zone centre. After that, the backfill over all 1,283 orders gives 1,244 exact, 8 estimated and 31 unlocatable, min 0.7 km, max 101.5 km, average 11.9 km.

Shown as a new Distance column on Seller Transactions (right of From/To, also in the CSV/XLSX export, with `~` marking an estimate) and as a chip beside the pickup location on the order detail. Also fixed the column the user flagged: the pickup name was wrapping onto two lines, so `.ftr__loc-line--from` is now nowrap with an ellipsis and the cell is 250–340px.

Backfill lives in `orders/management/commands/backfill_route_distance.py`, dry-run by default and safe to re-run, with `--all` to recompute and `--business` to scope.

**Files:** `delivery/geo.py`, `orders/models.py`, `orders/migrations/0056_order_route_distance_exact_order_route_distance_km.py`, `orders/signals.py`, `orders/management/commands/backfill_route_distance.py`, `workforce/views.py`, `workforce/templates/workforce/seller_transactions.html`, `workforce/templates/workforce/order_detail.html`, `workforce/static/workforce/css/fleet_transactions.css`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 16:18 — Page-notes help card rolled out across the whole staff dashboard
Turned the Pickup Automation notes card into a shared component and put it on every staff dashboard page. Built one `wfnote__` CSS block (round help button for navy heroes, `--ink` variant for light headers, the card itself, two reading columns collapsing to one on mobile) and one delegated click handler at the end of workforce.js that binds any button carrying `data-wfnote-toggle` to its panel — delegated on document so it keeps working after an HTMX swap and no page needs its own script. The shared `_page_header.html` include now renders the button itself when a page passes `notes_id`, which covered ten pages in a single edit. Pickup Automation was migrated onto the shared classes as the reference implementation.

Twelve subagents then wrote the per-page content in parallel over disjoint file sets, each reading the page's view and JS first so the cards describe real behaviour rather than guesses: what the page is, one line per control, the page's status/mode vocabulary, and a "Worth knowing" block for the things staff get wrong. 127 pages carry a card; `orders_api_guide` and `workflow_guide` were skipped (they are already documentation) and the dead `warehouses_list_backup` was left alone. Verified: every button's aria-controls matches a card id on the same page, all 130 dashboard templates compile, zero inline styles or `<style>` tags added, and a headless sample across five hero types opens and closes cleanly with no console errors and no horizontal overflow.

**Files:** `workforce/static/workforce/css/workforce.css`, `workforce/static/workforce/js/workforce.js`, `workforce/templates/workforce/parts/components/_page_header.html`, 127 staff page templates under `workforce/templates/workforce/` and `templates/delivery/`, `templates/wf_dashboard_base.html`

## 14:45 — Road distance via self-hosted OSRM + Location Review queue for GPS discrepancies
Replaced the straight-line distance with the real driving distance. Stood up OSRM in Docker (`ezzy-osrm`, bound to 127.0.0.1:5001, `--restart unless-stopped`): Geofabrik has no standalone Qatar extract, so the 252 MB GCC-states file is clipped to a Qatar bounding box with osmium — 27 MB, a 9× reduction — then run through the MLD car pipeline. Preprocessing peaked at 717 MB RAM and the whole build took under a minute.

`delivery/routing.py` is the client; every failure returns None so the caller falls back to the straight line and a routing outage can never block an order save. `delivery/geo.py` now returns `(km, exact, source)` and the order stores which method produced the figure (`route_distance_source`: road / straight line / zone estimate, migration orders/0057), so no screen has to guess whether it is showing a measurement or an approximation. Backfilled all 1,283 orders: **1,252 now carry real road distances**, 31 have no locatable endpoint. Average delivery moved from 11.9 km straight-line to 15.3 km by road — a median road/straight ratio of 1.34, so the old figure was reading about a third short. Page renders read the stored value and never call the router.

**New Location Review queue** (`/workforce/fleet/location-reviews/`, sidebar under Fleet A/Cs). Comparing each driver's delivery GPS against the customer's saved pin showed a median gap of 4.2 km, so one of the two points is routinely wrong and nothing can tell which but a person. `DeliveryLocationReview` (migration delivery/0042) opens a row when a delivery completes 1 km or more from the pin (`DELIVERY_GPS_FLAG_KM`), recording both points and the GPS accuracy; the delivery signal opens it and an already-decided review is never reopened. The console shows both coordinates as map links with the gap leading the row, and staff pick a verdict in bulk. Only "Driver GPS correct" changes data — it rewrites the order coordinates as `by_driver` and the stored road distance recomputes off the corrected pin; every other verdict is recorded without touching the address, because saying the customer pin was right is a statement about the driver, not a correction to the order.

Backfill opened **562 reviews** from past deliveries: 45 in the 1–2 km band, 122 at 2–5 km, 328 at 5–20 km and 67 over 20 km. Verified the decision path on the worst one inside a rolled-back transaction: an order pinned 99.2 km away by road, driver delivered at a point 100 km from that pin; accepting the driver's GPS moved the pin and the distance fell to 21.1 km. Also relabelled the status timeline's GPS figure to "N km from drop" with a walking icon — it is driver-to-drop proximity evidence, and it was using the same route icon and bare "km" as the new trip distance.

**Files:** `delivery/routing.py`, `delivery/geo.py`, `delivery/models.py`, `delivery/signals.py`, `delivery/migrations/0042_deliverylocationreview.py`, `delivery/management/commands/backfill_location_reviews.py`, `orders/models.py`, `orders/migrations/0057_order_route_distance_source.py`, `orders/management/commands/backfill_route_distance.py`, `ezzydelivery/settings.py`, `.env`, `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/delivery_location_reviews.html`, `workforce/templates/workforce/seller_transactions.html`, `workforce/templates/workforce/order_detail.html`, `workforce/templates/workforce/parts/status_timeline.html`, `workforce/templates/workforce/parts/sidebar/fleet_accounts.html`, `workforce/static/workforce/css/delivery_location_reviews.css`

## 15:10 — Location Review: compare the two points on the existing Leaflet map
The review rows linked each coordinate out to Google Maps, which was the wrong shape for the job — the task is to *compare* two points, and two separate browser tabs make that harder. Leaflet 1.9.4 is already loaded on every staff page by `wf_dashboard_base.html`, so the external links are gone and each row now opens an inline map showing both pins with a dashed line between them, auto-fitted to the pair, using the same CartoDB light tiles as the staff live map. Maps are built on first open and reused, so a page of 25 rows costs nothing until a row is opened. Marker colours match the coordinate icons in the table so the two readings agree.

While adding the routing config I appended `OSRM_ENABLED` etc. to `ezzydelivery/.env` — but the live file is the repo-root `.env`, and creating that second file shadowed it for python-decouple, leaving `ALLOWED_HOSTS` unset. Caught immediately; the running workers had already loaded settings so the site never dropped below 200. The stray file was removed and the config appended to the real root `.env` (backed up first as `.env.bak.20260729`).

**Files:** `workforce/templates/workforce/delivery_location_reviews.html`, `workforce/static/workforce/css/delivery_location_reviews.css`, `.env`

## 15:55 — Inline maps everywhere: one shared component replaces the Google Maps links
Rolled the Location Review page's inline map out across the site as a reusable component, since Leaflet was already loaded and every coordinate on every staff screen was linking out to Google Maps instead.

New `templates/includes/components/_map_point.html` plus `webpages/static/webpages/{css/map-point.css,js/map-point.js}`. The JS is a delegated click handler that **loads Leaflet on demand** if the page does not already have it, which is what lets one include work across `wf_dashboard_base` (has Leaflet) and `business_dashboard_base` (does not) without editing every base. It supports a single point or two points compared, drawing the line between them, and it stops event propagation so opening a map inside a click-through list row does not also navigate to the record.

Converted the coordinate links on: staff + seller order detail, order detail panel and content partial, delivery task detail (including the completion point, now compared against the customer pin), the orders list, follow-up list and tasks table, the status timeline's three GPS points (each compared against the delivery location), hub batch detail, seller detail, driver profile registration location, temp verify queue, zone areas (template and its AJAX rebuild), export page, and the QNAS lookup/test tools. Where a row had two links to the same coordinate, the pin now opens the map and the coordinates read as plain text rather than a second link to the same place. Rendering checks: 0 Google coordinate links left across all of them, 173 inline map toggles now rendered.

Deliberately left alone: the **17 driver-navigation deep links** in the fleet PWA and task navigation (`/maps/dir/?api=1&…&travelmode=driving`) — those launch real turn-by-turn navigation and Leaflet cannot replace that; place-name *searches* (`?q=<address text>`), which are not coordinates; the merchant-supplied `google_maps_link` on pickup locations; and the "paste a Google Maps link" input placeholders.

Also folded the Location Review page onto the shared component and deleted its bespoke map JS, so there is one implementation rather than two, and corrected the zone-areas page notes that still described opening Google Maps in a new tab.

**Files:** `templates/includes/components/_map_point.html`, `webpages/static/webpages/css/map-point.css`, `webpages/static/webpages/js/map-point.js`, `templates/wf_dashboard_base.html`, `templates/business_dashboard_base.html`, `templates/delivery/zone_areas.html`, plus the converted templates across workforce, orders and business and the button resets in `workforce/static/workforce/css/workforce.css`

## 16:30 — Route distance on every order surface + a circuit breaker on the routing calls
Put the OSRM driving distance on the rest of the order screens, and fixed a robustness hole the routing work had opened.

**The hole:** every order save now asks OSRM for a distance. On localhost that is a couple of milliseconds, but if the container is down each call waits out the 3-second timeout — a bulk import of 500 orders would have sat through 500 of them, about 25 minutes of dead waiting for a figure that is decorative. `delivery/routing.py` now carries a circuit breaker: three consecutive failures pause lookups for 60 seconds and every call falls straight through to the straight-line figure. Verified by pointing it at a dead port — eight calls during a simulated outage returned in 0.00s instead of eight timeouts. A health check counts as an explicit retry and clears the breaker.

**The display:** new `templates/includes/components/_route_distance.html` reads the stored figure (never routes during a render) and marks a fallback with `~` and a muted treatment. Added to the orders list, follow-up list, tasks table, delivery task detail, seller order details and detail panel, the staff order-detail content partial, and the Location Review queue — where the trip length gives the GPS gap its context, since a 2 km gap on a 3 km delivery means something very different from the same gap on a 40 km one. Also added a Distance column to the orders CSV export. The order detail page's own chip, written earlier today, was folded onto the shared component so there is one implementation rather than two.

Left as a dedicated table column: Seller Transactions, where a right-aligned numeric column is the right shape for a ledger rather than an inline chip.

**Files:** `delivery/routing.py`, `templates/includes/components/_route_distance.html`, `webpages/static/webpages/css/map-point.css`, `workforce/views.py`, `workforce/templates/workforce/order_detail.html`, `workforce/templates/workforce/delivery_location_reviews.html`, `workforce/templates/workforce/parts/delivery_task_detail.html`, `workforce/templates/workforce/parts/_order_detail_content.html`, `workforce/templates/workforce/parts/lists/orders_list_view.html`, `workforce/templates/workforce/parts/lists/dl_list_followup.html`, `workforce/templates/workforce/parts/components/_tasks_table_view.html`, `orders/templates/orders/order_details.html`, `orders/templates/orders/parts/order_detail_panel.html`

## 16:55 — Fixed template comments that were rendering as visible page text
Django's `{# ... #}` is a **single-line** comment. Written across two lines it is not a comment at all — the prose and the braces render straight onto the page. Nineteen of them had gone out today, so staff were reading things like `{# The pin in the zone line above opens the map for this row; these are the same coordinates, so they read as text, not a second link. #}` sitting in the middle of the orders list, right next to the coordinates.

All 19 converted to `{% comment %} ... {% endcomment %}` across 12 templates — the orders list, follow-up list, status timeline, task detail, order detail, seller transactions, client charges, the payout desk, payout history, location reviews, and the two business finance templates. One of them (`tasks_live_map.html`) predated today's work. Verified by rendering ten pages and counting literal `{#`/`#}` in the response: zero, down from the leaked prose being visible on several.

Added `scripts/check_template_comments.py` and wired it into `scripts/predeploy.sh` as its own gate step, so this cannot ship again — confirmed it exits 0 on the current tree and 1 against a deliberately broken probe file.

**Files:** `scripts/check_template_comments.py`, `scripts/predeploy.sh`, and the 12 templates listed above

## 17:10 — Yellow that was meant to be navy, and a real yellow ink token
The route-distance chip and the seller-transactions pickup line were rendering yellow instead of navy. The cause is a token trap: `var(--brand-primary, #001f3f)` reads as navy, but `--brand-primary` is defined — it is Ezzy Yellow `#f7c000` — so the navy fallback never applies and the text came out yellow at 1.68:1 on white, which is effectively invisible. Both now use `--brand-navy` directly.

Because yellow text is a recurring want, added `--brand-primary-text: #8f6b00` to the Brand Kit — the same hue darkened to 4.92:1 on white (WCAG AA body text), measured rather than guessed. Raw `--brand-primary` stays correct for yellow text on navy, where it is 9.86:1. The rule and the fallback trap are now written into `.claude/skills/brandkit.md`, whose own navy-hero recipe was the one sanctioned yellow-on-navy case and is annotated as such.

Cache-busters bumped on all three edited stylesheets (map-point, fleet_transactions, brandkit), collected and reloaded; site 200.

**Files:** `webpages/static/webpages/css/brandkit.css`, `webpages/static/webpages/css/map-point.css`, `workforce/static/workforce/css/fleet_transactions.css`, `.claude/skills/brandkit.md`, `templates/includes/head.html`, `templates/wf_dashboard_base.html`, `templates/business_dashboard_base.html`

## 17:14 — Swept the rest of the yellow-that-meant-navy across four apps
Audited the whole tree for the same token trap and found 22 more instances on pages untouched today. Every one was written `var(--brand-primary, #001f3f)` — the navy fallback proves navy was the intent, but since the token is defined as Ezzy Yellow the fallback never applied and all 22 rendered yellow. Nine were text at 1.68:1 on white, and two were white-on-yellow buttons (the staff test-page button and the live-map active filter), both effectively unreadable.

All 22 swapped to `--brand-navy` with the fallback left identical, so this restores what the authors wrote rather than redesigning anything. Affected: seller live tracking, returns list/detail, seller reports, the staff live map, the workforce test page, the delivery-list sort headers, and one border in the fleet PWA. Cache-busters bumped on all seven stylesheets — three of them had no `?v=` at all and now do. Verified zero occurrences remain in the served CSS; site 200.

**Files:** `business/static/business/css/{business-returns,business-live-tracking,business-reports}.css`, `workforce/static/workforce/css/{workforce,workforce-pages,live-map}.css`, `fleet/static/fleet/css/fleet-pwa-modern.css`, plus the eight templates carrying their link tags

## 14:17 — Payout console: shared navy hero + stat cards across all four pages
Replaced the four hand-rolled page mastheads on the payout console with one shared component pair in `workforce.css`: `wfhero__` (navy band, yellow 3px rule, eyebrow + title + sub left, tallies and ghost actions right — same geometry as the existing `ppl__hero`) and `wfstat__` (the headline-figure cards below it, with `--here` for where the money is sitting, `--on` for the filter currently applied, and `--done`/`--null` for quieter figures). Driver payout, client charges, client payout and payout history now all open the same way instead of each inventing its own header.

The driver payout page's four-gate "ladder" became four stat cards keeping the same links and marking; client charges' summary strip became cards that double as the status filter; the two client-leg pages gained cards they never had (COD held / charges to withhold / awaiting payout / accounts, and COD released / withheld / net transferred / voided — the last set agrees with the table footer, voids excluded). Notes-card copy on the worksheet was updated off "ladder gates" onto "stat cards" so the help text matches the page.

Pruned the CSS the conversion orphaned — 53 rules across the three page stylesheets, chosen by comparing every `dpw__`/`ccv__`/`bcp__` selector against actual template and JS usage rather than by eye, so nothing still referenced was touched. Verified by rendering all four pages as staff (200, one hero and four cards each) and screenshotting them before and after the prune at 1440px plus the worksheet at 390px — desktop pixel-identical, mobile stacks to one column. Cache-busters bumped on all four stylesheets; collected and reloaded; site 200.

**Files:** `workforce/static/workforce/css/workforce.css`, `workforce/static/workforce/css/{driver_payout_worksheet,client_charge_verification,cod_business_settlement}.css`, `workforce/templates/workforce/{driver_payout_worksheet,client_charge_verification,cod_business_settlement_report,cod_business_payout_history}.html`, `templates/wf_dashboard_base.html`

## 17:42 — Orders-list filter bar: dead date buttons, wrong Clear target, and a redesign
Working on the filter section of /workforce/orders/fulfilled-clients/ turned up three things that were broken rather than merely plain.

**The date presets did nothing.** Six views share `parts/lists/orders_list_view.html`, but only three ran the shared `apply_order_list_filters` helper. `fulfilled_clients_orders` and `non_fulfilled_clients_orders` hand-rolled their filtering and never read `dateFrom`/`dateTo`/`datePreset`, so the buttons rendered, the JS put the params in the URL, and the server ignored them — clicking "Today" reloaded an unchanged list and the button did not even stay pressed. Both now use the helper, keeping their own base queryset and their business-scoped picker. Covered by new tests in `workforce/tests.py`.

**"Clear" left the page.** Both the ✕ button and "Clear All" pointed at `wf_orders_all`, so clearing filters on any of the five scoped lists silently navigated to All Orders. They now clear to `request.path`.

**Export was a dead button on five of six pages.** Only `all_orders` supplies `export_columns_context`, so elsewhere the modal opened with no columns and no URL. Wiring it up would have been worse — `export_orders_csv` starts from every order and knows nothing about "fulfilled clients", so it would return rows the page never showed. The button and its modal are now conditional on `export_columns`. Scoping that endpoint per page is still open.

**The redesign.** One card instead of two stacked ones: the applied-filter strip folded into `.pg-filter` as `.pg-filter__applied`, and the loose `.active-filters-*` classes retired in favour of the `pg-filter__` prefix. The six date buttons became a real segmented control; fields are capped rather than greedy so a free-text box no longer spans half the viewport, and the status pair right-aligns under the row-1 actions. Yellow now appears exactly once — the rule on the applied strip — so the accent means "this list is filtered"; the green Bootstrap export button and the yellow-on-navy button trim are gone. Chips show real labels read off the models' choice lists (`Out for Delivery`, not `out_for_delivery`) via a new `filter_display_labels`. Rows wrap instead of crushing, every control got an aria-label, and on mobile the segmented control unjoins and wraps (it was clipping "Custom"), inputs go to 16px to stop iOS zooming, and icon buttons reach 44px.

**Files:** `workforce/views.py`, `workforce/tests.py`, `workforce/templates/workforce/parts/lists/orders_list_view.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 14:55 — Staff list filter bar: Select2 pickers were eating a row each, plus a design pass
The filter bar on the orders lists (Fulfilled Clients and the rest of the `pg-filter` pages) was rendering five rows deep instead of two. Cause: the global Select2 initialiser hides the native `<select>` and inserts a sibling container, so every width rule in `workforce.css` was landing on a `display:none` element while the visible picker kept the `width:100%` that `select2-filters.css` sets for the stacked filter forms elsewhere. Inside a flex row that meant Business, Order Status and DL Status each claimed a full line. Sized the container instead, mirroring the native flex widths — keyed off `.pg-filter__select-biz` rather than an id, since the print-labels bar uses the same block with `plf_business`.

Second problem behind the same symptom: `select2-filters.css` builds a 34px control to suit the taller bars, but this bar runs 1.75rem (28px) fields, so the pickers stood proud of the text inputs and nothing shared a baseline. The pickers now take the bar's own metrics — height, font, border, radius and the same yellow focus ring the inputs use — and the date segmented control was pinned to 1.75rem as well, since its own padding left it 4px short.

Design pass on top: added the Brand Kit's uppercase micro-label treatment (0.6rem/700, letter-spacing .07em, grey-500 — the vocabulary the applied-strip label already used) to name each cluster rather than each field, so the bar is scannable without a second line of chrome. Rows now read `CLIENT · picker · DL Code · Name/Mobile · actions` and `PERIOD · presets │ STATUS · pickers`, with both labels starting at the same x so the two rows share a rhythm. Brought the print-labels bar onto the identical vocabulary — it was still using raw `btn btn-dark` / `btn-outline-secondary` inside a `pg-filter` card — and gave its icon-only controls aria-labels. Dropped `.pg-filter__cal`, now unreferenced.

Verified at 1440 / 1024 / 390px: every control measures 28px on one baseline per row, the bar is back to two rows, and mobile stacks as before. Cache-buster bumped, collected, reloaded.

**Files:** `workforce/static/workforce/css/workforce.css`, `workforce/templates/workforce/parts/lists/orders_list_view.html`, `workforce/templates/workforce/parts/lists/print_labels_view.html`, `templates/wf_dashboard_base.html`

## 15:03 — Print Labels: design pass on the packing bench
This page had never had one — raw Bootstrap `table table-sm`, `btn btn-dark` / `btn-outline-dark` / `btn-primary`, `text-muted small` for every secondary line, and two inline `style=` attributes. The worst of it was the order numbers: they were plain links, and the staff dashboard sets `--bs-link-color: #ffd33b !important`, so the single most-scanned column on a page whose whole job is matching a parcel to a number was rendering Ezzy Yellow on white at 1.68:1. Same trap swept across four apps earlier today; this page was carrying it too.

Built a `plb__` layer in a new `print_labels.css` and rebuilt the page on it. The table is now the Brand Kit console ledger — navy header, hairline rows, grey wash on hover — with order numbers in navy monospace, phone/zone/date in mono, and COD amounts bold except zero, which steps back to grey because zero means already-paid rather than an amount worth reading. Ticked rows carry a faint yellow wash that agrees with the Brand Kit's yellow tick (which is `!important`, so the page defers to it rather than fighting it). The bulk button and its selection count moved into one strip instead of a button with a sentence beside it, and the button states its disabled reason by looking disabled. Empty state moved onto the Brand Kit recipe — one muted icon, one line, one way back — replacing a `fa-4x` icon and a Bootstrap-blue button. Both inline styles are gone.

The "showing the last 3 days" note was a loose sentence under the filter card; it now uses the shared `pg-filter__applied` strip so it reads as part of the bar. Added `pg-filter__hint` for that plain-sentence case.

Mobile: seven columns can't survive a phone and this page is used on one, so the ledger collapses to a card per order with labelled lines and the tick/print controls on the card's top-right corner. Verified at 1440px and 390px, plus the empty state and the selected state; site 200.

**Files:** `workforce/static/workforce/css/print_labels.css` (new), `workforce/templates/workforce/parts/lists/print_labels_view.html`, `workforce/static/workforce/css/workforce.css`

## 15:06 — Fix: published orders silently stuck with no delivery task (zone-mismatch AttributeError)
Three orders (EDN132-F0028/F0029/F0030-AB266/267/268) sat at "Published" with `task_status=dl_task_listed` but no `DeliveryTask` row at all. Root cause: the zone-mismatch annotation added in `d6b08c9c` writes `address_update.notes`, but `DlAddressUpdate` never had a `notes` field — so any order whose human-placed pin resolved to a different zone than the typed one raised `AttributeError` inside `_create_delivery_task_from_order`, where the broad `except Exception` swallowed it and returned `None`. The publish view had already set `task_status='dl_task_listed'` before saving, so the order looked listed while `task_created` stayed False and no task existed.

Added the missing `notes` TextField to `DlAddressUpdate` (migration `0043`) so the mismatch flag actually persists, and wrapped the whole zone reverse-resolve/mismatch block in its own try/except — that check is advisory and must never be able to abort task creation. Backfilled the three orders by re-running task creation: tasks 1379/1380/1381 created, labels + QR generated, each address record now carrying its `[ZONE-MISMATCH]` line. `orders`/`delivery` suites show the same 4 pre-existing failures before and after (warehouse inventory + `original_order_data` int/str), so no regression. Server reloaded, site 200.

**Files:** `delivery/models.py`, `delivery/migrations/0043_dladdressupdate_notes.py`, `orders/signals.py`

## 15:24 — Follow-Up list: hero counters + driver filter moved into one right-hand cluster
The Follow-Up Tasks hero had the staleness counters floating in the middle (space-between with three children) and the driver picker sitting alone on the filter bar below. Wrapped the counters, the driver select and the notes button in a new `wfl__hero__aside` so the whole cluster reads as one group pinned to the right of the title, and pulled the "All Drivers" select up out of the filter form — it stays owned by that form through the HTML `form="followupFilterForm"` attribute, so `this.form.submit()` and the round-trip `?driver=` state work unchanged. Dropped the inline `max-width` style it was carrying.

Two things had to be handled for the picker to sit right. First, the staff dashboard turns every filter select into a Select2, which hides the native control and inserts a sibling container — so the width/height rules had to target `.select2-container.ez-filter-select` (with `width: auto !important`, since Select2 writes width inline), same technique as the orders filter bar. It's now 1.75rem tall, matching the notes button, on the brand kit's dark-surface field treatment (translucent white fill, white chevron, yellow focus border) with a white-on-navy label. Second, the aside was shrink-wrapping to its own content and breaking the picker onto a second line, so it takes `flex: 1 1 auto` with `justify-content: flex-end` — hero height dropped from 93px to 53px.

Verified in a real browser at 1440px and 390px: single row on desktop, stats + full-width picker on mobile, dropdown opens and reads correctly over navy, picking a driver reloads with the filter applied and the selection preserved, notes button still toggles.

**Files:** `workforce/templates/workforce/parts/lists/dl_list_followup.html`, `workforce/static/workforce/css/followup-list.css`

## 15:29 — Print Labels filter rebuilt on the orders-list structure
Two problems behind "the filter section is still wrong". First, the bar wrapped: every field's flex-basis was its full desktop width, and `flex-wrap` breaks a line as soon as the bases overflow rather than shrinking first, so below ~1200px the whole bar dropped to two and three rows. Measured on the live site it wrapped at 1100px and stacked six-deep at 768px. Each field is now a small basis plus a max-width cap — the cap is the old desktop size so a wide screen is unchanged, while the small basis is what a laptop needs. Verified 1024–1536px on both this page and the orders lists: one line per row throughout.

Second, and the actual ask: this page's filter did not match the others. It was a single row ending in two raw From/To date boxes, where every orders list uses a two-row bar with a segmented Period control. Rebuilt it on that structure — row one is Client, search and the apply/clear actions; row two is Period with Today / Yesterday / 3 Days / Last Week / Month / Custom, and the From/To boxes now live behind Custom exactly as they do elsewhere. It reuses the shared `pgf_*` ids so the existing handler in workforce.js drives it with no page-specific script, and the page's last-3-days default lights the 3 Days button on arrival.

That handler needed one fix to be genuinely shared: `pgFilterSubmit` read `.value` off `pgf_dlCode`, `pgf_cStatus` and `pgf_dlTaskStatus` unconditionally, which this page does not have, so it threw before building the URL. Reads now go through a null-safe helper. The view passes `datePreset` through so the right button is lit, and the notes card and the Showing strip were reworded off "From and To boxes" onto the period control.

Verified live with a real staff session: presets navigate and light correctly, Custom reveals the date boxes, no console errors.

**Files:** `workforce/templates/workforce/parts/lists/print_labels_view.html`, `workforce/static/workforce/js/workforce.js`, `workforce/static/workforce/css/workforce.css`, `workforce/views.py`, `templates/wf_dashboard_base.html`

## 15:41 — Select2 search field now matches the input box of the bar it filters
The search field inside every filter picker stood taller than the inputs beside it. Two causes. It was pinned to 30px in a 0.4rem-padded box with a 0.3rem radius, none of which is the brand filter-field box; and the 30px never even applied, because `mobile-app.css` puts a `min-height: 2.75rem` touch floor on `input[type="search"]` from 576px up — Select2's field is a search input, so it rendered 44px on desktop. Added the `min-height: 0` release valve the filter bars already use, and moved the field onto the brand box: 0.375rem wrapper padding, `0.1875rem 0.5rem`, 0.78rem, radius-sm, grey-450 placeholder, and the same 3px yellow focus ring the filter inputs carry.

Height could not be one global number. The dropdown is parented to `<body>`, so CSS cannot see which bar it came from, and the bars are not one height — 28px on the console filter rows, 34–36px on the older stacked forms. A fixed value is oversized on one page and undersized on the next. So the initialiser now measures the picker's own control at enhance time and writes `--ez-filter-field-h` onto that picker's dropdown; the CSS reads it with the 1.75rem brand height as fallback, and a picker built inside a hidden panel (measures 0) keeps the fallback rather than collapsing.

Verified in a browser on both shapes: All Orders (console bar) gives search field 28px against 28px picker and 28px bar inputs — identical font and radius; Purchase Orders (stacked form) gives 34px against its 34px picker and 36px neighbours.

**Files:** `webpages/static/webpages/css/select2-filters.css`, `templates/includes/main_dashboard_scripts.html`, `templates/wf_dashboard_base.html`, `templates/business_dashboard_base.html`, `templates/fleet_dashboard_base.html`, `templates/wh_dashboard_base.html`

## 16:25 — Two real defects in the Print Labels period filter
Testing the bar properly, rather than comparing it to the orders list, turned up two faults in the filter I had just shipped.

The lit preset lied about the applied range. On arrival the page showed 46 orders with the "3 Days" button lit; pressing that same lit button gave 35. The view's default ran `now - 3 days` with no upper bound — four calendar days, open-ended — while the preset in workforce.js is today-2 → today. The default now computes exactly what the button computes, via `timezone.localdate()` rather than `now().date()`, so the window does not slip a day between midnight and 03:00 Qatar time. Default and button now both return 35.

Paging dropped the preset. The page links carried search, dateFrom and dateTo but not datePreset, because `filter_params_list` never appended it — the orders list does. Results stayed correct since the dates travelled, but the control forgot itself: page 1 lit "Month", page 2 lit nothing. Now carried; verified page 2 keeps it lit.

Both were mine, introduced when the segmented Period control replaced the raw From/To boxes — before that this page had no preset to be wrong about.

**Files:** `workforce/views.py`

## 16:48 — Reusable staff filter component + the Print Labels productivity gaps
Turned the filter bar into a shared component with a per-page extension point, then closed the gaps the audit had listed.

**The component.** `parts/components/_page_filter.html` renders the core every staff list needs — client picker, search, Period presets, apply/clear, the applied-chip strip and a page-size control — and takes `extra_filters_template`, a template path rendered inside the form at the end of row 2 with the full page context. A page adds only what is genuinely its own; the core cannot drift between pages again. Print Labels' own filters (printed state, order status, sort) now live in `_print_labels_filters.html` and nothing else about the bar is page-specific. Every param is documented in the component's own header.

**Printed state.** Order gained `label_printed_at` + `label_print_count` (migration 0058) and `wf_print_waybill` stamps whatever it actually rendered. That makes "Not printed yet" a real filter — previously nothing recorded that a label existed, so duplicates were structural. Reprints bump the count rather than being blocked.

**Selection across pages.** Ticks are held in sessionStorage keyed to the current filter, and ids ticked on pages you have left ride along as hidden inputs, so a client spanning four pages is one print job. The count states how many are off-page. Changing the filter starts a fresh selection, because the old ticks belonged to a different list.

**Print all matching.** Appears once the filter matches more than a page; hands the filter to the print view instead of a list of ids, capped at 500. The print view rebuilds the list through the same `_print_labels_queryset` the page uses, so what prints is by construction what was on screen. Verified: "Print all 70 matching" rendered 70 waybills.

**Also:** status filter (cancelled orders were printable), multi-client selection, sort, per-page (10/25/50/100 — the backend already accepted it and no UI had ever exposed it), and server-built removable filter chips.

**Plumbing.** Deleted `FilterManager` — 237 lines of a second filter engine initialised on every page but keyed on ids no template has rendered in a long time, so its chips never drew and its localStorage presets were unreachable. It also bound a second Enter handler on the filter form; both fired on one keypress and happened to agree on the URL. `pgFilterSubmit` now handles repeated values (a multi-select returned only its first option) and reads the checkbox-dropdown filters. The global Select2 initialiser skipped `multiple` entirely; it now enhances them inside GET filter bars only, which is where the searchable tag box is wanted — no other page in the tree has a multi-select, so nothing else is touched.

**One more date defect.** A `datePreset` arriving without dates (bookmark, hand-typed URL) lit the button but applied the page default — the same lie as this morning's, by a different route. `_resolve_print_label_dates` now resolves preset → range server-side, shared by the page and the queryset builder. `?datePreset=week` and clicking Last Week both return 70.

Verified live with a staff session: filters, chips, cross-page selection, both print paths, stamping, 1024–1536px and mobile, no console errors. Orders list regression-checked.

**Files:** `orders/models.py` + migration `0058`, `workforce/views.py`, `workforce/static/workforce/js/workforce.js`, `templates/includes/main_dashboard_scripts.html`, `workforce/static/workforce/css/{workforce,print_labels}.css`, `workforce/templates/workforce/parts/components/_page_filter.html` (new), `workforce/templates/workforce/parts/lists/_print_labels_filters.html` (new), `workforce/templates/workforce/parts/lists/print_labels_view.html`, `templates/wf_dashboard_base.html`

## 16:55 — Client field on Print Labels was rendering blank
The multi-select client picker read as an empty white box: no placeholder, no arrow, nothing to say it was a picker at all. Select2 4.1 builds the inline search of a multi-select as an auto-sizing `<textarea>`, which computed 88px tall inside a 28px control, so the "All Businesses" placeholder was rendering below the visible strip. Pinned to one line and given the same grey as the text inputs beside it, plus a caret — without one it read as a text box, and every other picker on the bar has one.

Picking clients then showed two more faults. Select2 positions the tag's remove button absolutely and reserves no room for it, so the × sat on top of the first characters ("×83 MarkFairWhale"); and the rendered list defaults to `overflow: visible`, so a second tag spilled straight out past the border of the control. Tags now reserve room for the ×, truncate a long trading name with an ellipsis, and stay inside the box. Checked at one, two and three clients: nothing escapes and the row holds 28px.

**Files:** `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 17:08 — Added a "2 Days" period preset (today + yesterday)
The date section jumped straight from Yesterday to 3 Days, and Yesterday is that one day alone — no way to see today and yesterday together, which is the window a packing bench works to when it clears the overnight orders. Added a **2 Days** button between them.

It sits in the shared filter component, so both the Print Labels bar and the orders lists get it, and every server-side preset resolver learned it: the two `preset_days` maps the orders lists use, the Print Labels resolver, and `DATE_PRESET_LABELS` so the applied chip reads "Today + Yesterday" rather than the raw key. The JS preset arithmetic matches (today-1 → today).

Verified live: clicking it returns 17 orders, which is today's 5 plus yesterday's 12; a bare `?datePreset=2days` with no dates resolves server-side to the same 17, so the lit button and the applied range agree by either route. Both bars still hold one line per row and the orders list picks it up unchanged.

**Files:** `workforce/views.py`, `workforce/static/workforce/js/workforce.js`, `workforce/templates/workforce/parts/components/_page_filter.html`, `workforce/templates/workforce/parts/lists/orders_list_view.html`, `templates/wf_dashboard_base.html`

## 17:16 — Blinking cursor in the Client filter
Clicking the client picker parked a blinking text caret in a field that still read "All Businesses". Select2 builds a multi-select's search as a full-width `<textarea>` inside the control, so the whole box behaves as a text input — while every other picker on that bar is a single-select whose search lives inside the dropdown and shows no caret at all. The client filter was the only one flashing a cursor at you.

Hidden while the placeholder is showing, via `:placeholder-shown { caret-color: transparent }`. The moment anything is typed the placeholder goes and the caret comes back, so typing never feels dead. Checked: focused-and-empty has no caret, typing "mav" restores it and narrows to Maven Abaya, Enter selects it, and the caret goes quiet again.

Ruled out first — no CSS animation on the control or its parts, no autofocus on load (activeElement is body), no oscillation in the field's geometry over 2s, and no layout flash during load: the raw select is already 28px before Select2 replaces it.

**Files:** `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 14:32 — COD payout invoice masthead (logo + centred title + issuer block)
The business COD payout invoice had no branding at all. Added a centred masthead at the top of the printed sheet: the round Ezzy logo mark over an uppercase "COD Payout Invoice" heading, sitting above the head rule. The left side of the header now carries EzzyDelivery's own identity (name, 66451589, admin@ezzydelivery.qa) instead of the client name, and the business being paid moved to a "Payout to" block under the header so it still reads prominently. New `bpi__masthead` / `bpi__logo` / `bpi__doc-title` / `bpi__issuer-*` rules added to the shared invoice CSS, including print sizing and break-inside guards.

**Files:** `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/static/workforce/css/cod_payout_invoice.css`, `workforce/templates/workforce/driver_payout_invoice.html`

## 14:41 — Same masthead on the driver payout invoice
Applied the invoice masthead to the driver payout sheet so both payout documents look identical: round Ezzy logo over a centred "Driver Payout Invoice" heading, EzzyDelivery's name/phone/email on the left of the header, and the driver moved into a "Payout to" block beneath it. No new CSS — it reuses the `bpi__masthead` / `bpi__issuer-*` rules added earlier today.

**Files:** `workforce/templates/workforce/driver_payout_invoice.html`

## 14:49 — Invoice masthead: heading on top, larger logo beneath
Reordered the masthead on both payout invoices so the document heading sits first and the round Ezzy mark sits under it, still centred. The logo grew from 72px to 120px on screen (100px in print) so it reads as the sheet's anchor rather than a small badge.

**Files:** `workforce/static/workforce/css/cod_payout_invoice.css`, `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/templates/workforce/driver_payout_invoice.html`

## 14:58 — Invoice header rebuilt as three columns with the logo dead centre
Turned the payout invoice header into a 3-column grid: EzzyDelivery's contact block on the left, the document title over the round logo in the centre, and the invoice/payout meta on the right. The separate full-width masthead is gone — it is now the middle column. Below 768px the three columns stack with the logo block first and everything centred, and the logo drops to 96px on phones.

**Files:** `workforce/static/workforce/css/cod_payout_invoice.css`, `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/templates/workforce/driver_payout_invoice.html`

## 15:02 — Equal-width invoice header columns
Changed the invoice header grid from `1fr auto 1fr` to three equal thirds, so the issuer block, the centred logo/title and the meta list each take the same width instead of the centre column sizing to the logo.

**Files:** `workforce/static/workforce/css/cod_payout_invoice.css`

## 15:34 — Payout invoices now print on the company letterpad
Applied the Ezzy Delivery letterhead to both payout invoices. The stationery PDF was rendered at 300dpi and cropped into three static assets — the top band (black wedge, yellow sweep, company name, four contact items, round logo), the bottom band, and the faint bag watermark — which are now a full-bleed image at the head of the sheet, a full-bleed image at the foot, and the sheet's own background. The hand-built masthead, issuer block and three-column header were removed since the letterhead supplies all of that; the sheet now reads letterhead → centred document title → "Payout to" + invoice meta → table → summary → signatures → bottom band. Side padding moved into a `--bpi-pad-x` variable so the bands can cancel it and reach both paper edges in print.

Two bugs surfaced while verifying against the live pages. The print override used a bare `--bpi-pad-x: 0`, which made `calc(100% + 2 * var(...))` invalid and let the bands fall back to their intrinsic 1600px width, so the letterhead ran off the right edge of the printed page — fixed by giving the zero a unit. And `fleet_transactions.css`, which `wf_dashboard_base` loads on every workforce page, hid `footer, .footer` in print; that was swallowing the invoice signature strip on paper. Narrowed it to `#sticky-footer, .site-footer`, which is what the rule was actually for.

Note: the letterhead carries `info@ezzydelivery.qa`, not the `admin@` address asked for earlier — the stationery's own text won.

**Files:** `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/templates/workforce/driver_payout_invoice.html`, `workforce/static/workforce/css/cod_payout_invoice.css`, `workforce/static/workforce/css/fleet_transactions.css`, `templates/wf_dashboard_base.html`, `webpages/static/webpages/img/letterhead-top.png`, `webpages/static/webpages/img/letterhead-bottom.png`, `webpages/static/webpages/img/letterhead-watermark.png`
