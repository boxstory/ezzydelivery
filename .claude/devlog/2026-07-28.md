
## 08:12 — Renamed n8n conversation API → aiagent
Renamed the AI-agent conversation API from "n8n" to the vendor-neutral "aiagent" (it works for any client, n8n is just one). Module ai_agent/n8n_api.py → aiagent_api.py (classes N8n* → AiAgent*); URL prefix /api/ai-agent/n8n/ → /api/ai-agent/aiagent/ (names aiagent_*); settings + .env keys N8N_API_TOKEN/N8N_API_RATE_LIMIT → AIAGENT_API_TOKEN/AIAGENT_API_RATE_LIMIT (value preserved); auth header X-N8N-Token → X-AIAgent-Token; AI Config page card/fields/generate-button + wf_ai_config EDITABLE_KEYS updated; doc + memory renamed. Left the pre-existing N8N_*_WEBHOOK_* settings untouched (separate real n8n integration). Verified: new endpoint+header 200, old path 404, old header 401, config card renders, no code stragglers.

**Files:** `ai_agent/aiagent_api.py` (was n8n_api.py), `ai_agent/urls.py`, `ezzydelivery/settings.py`, `.env`, `workforce/views.py`, `workforce/templates/workforce/ai_config.html`, `.claude/docs/05-api-integrations/aiagent-conversation-api.md`

## 11:22 — QNAS verify now records accuracy + location changes land on the timeline
Clicking "Verify" on a delivery task saved the coords and qnas_status but never wrote coords_accuracy, so the Accuracy field never became "By Staff"/"Exact" and the order stayed in the location-issue queue; the save was also fire-and-forget so failures were silent. The verify call now sends the QNAS match precision (exact/street) plus a source label, the endpoint defaults any unlabelled staff save to "by_staff", returns the accuracy so the chip repaints without a reload, and surfaces a "Not saved" badge on failure. Separately, no location change was ever recorded anywhere, so the timeline could not show it — added a shared `orders/location_history.py` logger that writes a "Location Updated" OrderStatusHistory row (from → to pin + accuracy, who, and source) from staff QNAS verify, staff pin paste, AI parse, the customer verification link, the driver app and shared WhatsApp pins. The task timeline renders those with a pin icon and, for records that predate this logging, derives "Address Verified" / "Location Confirmed by Client" events from the stored verification stamps.

**Files:** `orders/location_history.py`, `orders/models.py`, `orders/migrations/0055_alter_orderstatushistory_field_name.py`, `orders/views.py`, `workforce/views.py`, `workforce/static/workforce/js/qnas-verify.js`, `workforce/static/workforce/css/workforce.css`, `workforce/templates/workforce/parts/delivery_task_detail.html`, `workforce/templates/workforce/parts/status_timeline.html`, `fleet/views.py`, `whatsapp/waha_views.py`, `templates/wf_dashboard_base.html`, `templates/business_dashboard_base.html`

## 09:06 — Pickup status column on the Follow-Up Tasks list
Added a "Pickup / Plan" column immediately before the DL Status column on `/workforce/tasks/followup-list/`, so staff can see the first-mile leg (PickupTask status, its disposition plan and the pickup driver) next to the delivery status. The view now select_relates `order__pickup_task` and its driver to keep the list at the same query count, and the column header sorts on the pickup status. Styling follows the page's ledger look — ink text with a single leading swatch keyed to the leg stage — so it doesn't compete with the coloured DL badge; rows with no pickup task render a muted dash.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/lists/dl_list_followup.html`, `workforce/static/workforce/css/followup-list.css`

## 09:12 — Narrowed the pickup column
The new Pickup column on the follow-up list was too wide, so it is now capped at 6.5rem with both lines truncating to an ellipsis. Long labels are shortened in place ("Arrived at Client" → "Arrived", disposition renders as Hub / Self / Transfer) and the full status, plan and driver stay available in the cell's tooltip.

**Files:** `workforce/templates/workforce/parts/lists/dl_list_followup.html`, `workforce/static/workforce/css/followup-list.css`

## 12:37 — COD/Fawran ledger gaps closed without moving any balance
Fixed nine gaps in the Fawran/electronic COD flow found while tracing the staff Fleet Transactions page. The big one: a mixed collection like `{cash: 200, fawran: 500}` recorded `payment_method='fawran'` (just the dominant leg), which auto-settled the whole task and erased 200 QR of real cash from the driver's liability — cash-in-hand, hand-in eligibility and the ledger buckets are now all split-aware via new `split_cash_leg()` / `task_cash_leg()` / `is_electronic_only()` helpers. Second: `record_transaction` derived its per-method balance snapshot from `DriverTransaction.delivery_task`, but deposits link through `DeliveryTask.cod_submission_txn`, so the "already settled" exclusion matched nothing and every new row was stamped with lifetime totals (driver 61's newest row read 144,414 instead of 4,165); it now replays the ledger straight after insert instead of waiting for the once-a-day page-load recalc. Also: partial delivery now marks electronic COD as with-Ezzy like the full-delivery path does, the additional-COD path passes its payment method, bank/ATM get their own running buckets, the two "Total" figures no longer use the `add` filter (it coerces with `int()`, so 100.60+50.60 rendered 150), the per-deposit split is one query instead of three per row, electronic collections show "With Ezzy" instead of a permanent "Pending", and a new `cod_reference` field captures the Fawran/POS transfer reference so a collection can be reconciled against the bank.

Verified against a full before/after snapshot: cod-in-hand (cached + live), wallet balance, pending earnings, 7-day earnings and the pending-COD task list are byte-identical for all four drivers. The only balance rows that moved were two that were provably wrong — driver 61's latest row 144,414/21,436 → 4,165/0 and driver 153's 0 → 1,485 — both now matching `live_cod_in_hand` exactly. 86 June rows shifted by 25 QR where the one existing mixed split moved to its correct bucket. Left alone deliberately: 59 legacy Fawran tasks (22,362 QR) that were swept into cash hand-ins before the electronic exclusion existed — correcting those would restate historical deposit amounts.

**Files:** `fleet/wallet_service.py`, `fleet/models.py`, `fleet/views.py`, `ezzy_api/views.py`, `delivery/models.py`, `delivery/migrations/0039_deliverytask_cod_reference.py`, `workforce/views.py`, `workforce/templates/workforce/fleet_transactions.html`, `fleet/templates/fleet/parts/task_detail_sheet.html`

## 13:18 — Legacy electronic-COD reconciliation report (read-only)
Added a staff report at `/workforce/fleet/cod-legacy-reconciliation/` covering the 59 Fawran/POS collections (22,362 QR) that were swept into driver cash hand-ins before submissions started excluding electronic methods. It groups them under the 10 affected deposits across 3 drivers and states, per deposit, what was declared as handed in versus the physical cash the driver could actually have handed over — the electronic legs are highlighted in the task table. Totals: 152,331 declared, 22,362 electronic inside it, 129,969 physical cash expected. It also surfaces a separate 875 QR anomaly on CODD-20260627-0001, where the declared amount exceeded the tasks the deposit actually settled, so that variance is not conflated with the sweep.

The page deliberately has no write path — no settle button, no adjustment, nothing that would restate a historical deposit — because driver balances are already correct (the electronic legs were never counted as cash-in-hand); what is overstated is only the recorded size of each hand-in, which is a cash-desk/bank matching job. Order COD status on the electronic legs is shown for information only (52 with Ezzy, 4 settled with business, 3 still flagged "with driver") and business payout was left untouched pending its own plan. CSV export reuses the shared choose-columns modal. Verified the whole report is side-effect free: a full before/after snapshot of balances, earnings and pending lists came back identical.

**Files:** `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/cod_legacy_reconciliation.html`, `workforce/static/workforce/css/cod_legacy_reconciliation.css`, `workforce/templates/workforce/cod_ledger.html`

## 13:52 — Driver-leg COD status aligned on 16 legacy orders
Sixteen orders (7,285 QR) still read `cod_with_driver` even though the driver had already settled them — including the 3 Fawran ones the reconciliation report flagged. All sixteen trace to three April deposits (181/220/221) that predate the line in `submit_cod_to_admin` which now sets `cod_with_ezzy` on settle, so it is a closed historical set with no recurrence risk. Fixed via a new `sync_cod_order_status` management command rather than an ad-hoc shell edit: it compares the order's COD status against the driver-leg truth on the task, is dry-run by default, and only ever moves the driver leg — `cod_settled_with_business` rows are never touched.

Left deliberately unapplied: one reverse-drift row (task 99, 210 QR) where the order says the money is with Ezzy but the driver has not settled it. Moving a status backwards is a different decision, so the command reports it and skips it unless `--include-reverse` is passed. Business payout is untouched and unaffected — that queue gates on `cod_settled`/`cod_client_settled` and never reads `cod_status_by_staff`, so it stayed at 652 tasks before and after. Verified with a full snapshot that driver balances, wallet, pending earnings, 7-day earnings and the pending-COD lists are unchanged; settled-but-still-with-driver is now zero.

**Files:** `fleet/management/commands/sync_cod_order_status.py`

## 15:05 — G3 + G4: COD submission approval and COD refunds made reachable
**G3.** The staff COD-submission workflow keyed on `transaction_type='cod_driver_settle'`, but `submit_cod_to_admin` only ever writes `'cod_deposit'` — so all five surfaces (the list plus edit / add-task / remove-task / approve) matched zero rows and `is_approved` was false on every row in the database. Added a `DriverTransaction.COD_SUBMISSION_TYPES` constant and pointed all five at it, so a hand-in is found whichever type it carries. Turning the list on immediately exposed a second bug it had been hiding — `driver.profile.get_full_name()` does not exist on Profile — replaced with a `_driver_display_name()` helper that falls back profile → user → driver code. The list now shows all 21 submissions, the status filters work, and an approve POST returns success end to end (verified inside a rolled-back transaction).

**G4.** The engine was in better shape than the audit suggested: `WalletService.record_cod_return` and the staff `process_cod_return` endpoint both existed with sound guards. What was missing was that **nothing in any template or script ever called them**, plus three correctness bugs. `live_cod_in_hand` excluded the whole task whenever a `cod_return` existed, so refunding 105 of a 210 collection would have wiped all 210 off the driver's liability — it now subtracts by amount. A partial refund also forced the order to `not_collected`; that now only happens on a full reversal. Processing a refund now closes out any open `ReturnRequest`, so `cod_reversal_processed` can finally become true. Added the missing UI: a "Refund COD to customer" action on the staff delivery-task page, shown only when COD was collected, no refund has been made, and the business has not already been paid — once Leg 3 has run the liability sits with the business, not the driver.

Also corrected what `cod_reversal_amount` means. It is the refund owed to the customer, but the partial-delivery path stored the *uncollected shortfall* (order 375, collected 300 → stored 75 for money never taken) and the business-return path stored the order's full face value regardless of whether anything was collected. Both now record only what can actually be handed back. The four existing return rows were left as-is rather than restating figures already shown to sellers — the refund UI computes its own ceiling from the task, so a wrong refund cannot be issued from them.

Verified: partial refund of 105 moved in-hand 1,485 → 1,380 exactly and kept the order status; full refund moved it to `not_collected`; a return request flipped to processed. All test writes rolled back. 13 COD tests pass. The only production data movement during this work was one live delivery (task 1358, 1,705 QR), whose ledger row stamped its running balance correctly at insert.

**Files:** `fleet/models.py`, `fleet/views.py`, `fleet/wallet_service.py`, `workforce/views.py`, `business/views.py`, `workforce/templates/workforce/parts/delivery_task_detail.html`

## 16:40 — Remaining COD gaps closed (G5, G7, G8, G9, G11, G12)
**G5 — wallet_balance drift.** `wallet_balance` was accumulated independently inside `record_transaction` from the *passed* payment method, which is null on ~14% of rows, so a Fawran collection recorded without one was debited as cash. Driver 61 had drifted 631 QR from `cod_in_hand`. Both fields are now re-derived together in `sync_cod_in_hand()` from the same task truth, and `record_transaction` calls it after the row exists rather than accumulating deltas — so no COD path can leave either field stale, and they cannot disagree again. Repaired driver 61 (−6,501 → −5,870); the other three drivers were already exact and did not move.

**G7 — meaningless balance rows.** The Fawran and POS "in hand" figures are structurally zero (electronic COD reaches Ezzy at collection), so they read as broken. Cash now carries an "in hand" qualifier and the electronic rows render only if they ever hold a value; the Total line only appears when it differs from cash.

**G8 + G9 — the two status ladders.** `cod_status_by_client` was written once at import and never advanced: 6 of its 9 values had never been used and 793 orders sat null. Added `orders/cod_status.py` as the single mapping from the operational ladder to the seller-facing one, and wired it into every leg — collection, hand-in, business payout and the payout reversal (the last three use bulk `.update()`, so a signal would never have fired). Backfilled 995 orders onto a real seller status and 314 never-touched COD orders from null to `not_collected`. One order with a collection but no status was deliberately left alone and named in the command output rather than guessed at.

**G11 — 95 ledger rows** given the payment method from their task (86 cash, 9 Fawran); one row has no task and stays null. Reads already preferred the task's method, so no balance moved — this fixes grouping and exports.

**G12 — task 99** (BMS045-#1993-AA028, 210 QR) flipped back to `cod_with_driver`: the order claimed Ezzy held money the driver still has.

All three commands are dry-run by default and idempotent on re-run. Verified with a full before/after snapshot: every driver's cod_in_hand, live in-hand, pending earnings, 7-day earnings and pending-task list unchanged; all three now reconcile exactly with wallet_balance. 13 COD tests pass, all five COD surfaces return 200.

**Files:** `fleet/wallet_service.py`, `orders/cod_status.py`, `orders/management/commands/backfill_cod_ladders.py`, `fleet/management/commands/backfill_txn_payment_method.py`, `ezzy_api/views.py`, `fleet/views.py`, `workforce/templates/workforce/fleet_transactions.html`, `workforce/static/workforce/css/fleet_transactions.css`, `templates/wf_dashboard_base.html`

## 14:40 — Payout invoices: itemised business COD payout + driver payout document
Closed the gap where the Business COD Payout screen counted out the **gross** COD while the backend quietly paid **net** of delivery fees — staff confirmed one figure and the business received another, and the net actually paid was never shown anywhere. The payout is now an invoice: every order lists its COD and its delivery charge, the two totals are stated, then each deduction is a line, then the net payable. Staff can add further charges at payout time (fulfilment, cargo handling, other) and the sticky tape shows COD → delivery charge → pay out so the on-screen figure always equals what the backend books.

Added `fleet.BusinessPayoutDeduction` (migration 0030) so each deduction line links the payout transaction to the revenue transaction it booked. `settle_cod_with_client()` takes a `deductions` list and books one revenue row per line; `reverse_cod_client_settlement()` now unwinds via those rows instead of matching loose `delivery_charge` transactions by reference number, with the old matching kept as a fallback for pre-existing payouts. Every payout now ends on a printable invoice at `/workforce/fleet/cod-business-settlement/invoice/<CODCS-code>/`, which also carries a void stamp if the payout was later reversed.

Driver side: a payout invoice at `/workforce/settlement/<id>/payout-invoice/` listing only the earnings being paid — COD is the hand-in leg, not money owed, so it never appears on the driver's document — reachable from any settled row on Driver Transactions. The staff Earnings Verification console keeps its bulk edit but each row now shows the COD collected and its method plus an expandable driver activity trail, so a row can be judged on the whole job before publishing. Finance sidebar (desktop + mobile) now carries the full set: Business COD Payout, COD Reconciliation, Earnings Verification, Driver Payouts — the payout page and the reconciliation report previously had no mobile nav entry at all.

Verified inside a rolled-back transaction on production data: 570 gross − 60 delivery − 25 fulfilment − 10 cargo = 475 net recorded exactly, three deduction lines with matching revenue rows, invoice renders 200 with every line, reversal returns exactly the 475 paid and unlinks all three tasks, nothing persisted. All seven touched pages return 200. Note for later: `DriverSettlement.gross_earnings` stores COD + earnings combined, so a COD-only settlement reads as "earnings" on the record itself — the new invoice reports the true earnings figure instead of that field.

**Files:** `fleet/models.py`, `fleet/migrations/0030_businesspayoutdeduction.py`, `fleet/wallet_service.py`, `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/cod_business_settlement_report.html`, `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/templates/workforce/driver_payout_invoice.html`, `workforce/templates/workforce/earnings_verification.html`, `workforce/templates/workforce/fleet_transactions.html`, `workforce/templates/workforce/parts/sidebar/finance.html`, `workforce/templates/workforce/parts/dashboard_sidebar_workforce_mob.html`, `workforce/static/workforce/css/cod_payout_invoice.css`, `workforce/static/workforce/css/cod_business_settlement.css`, `workforce/static/workforce/css/earnings_verification.css`, `workforce/static/workforce/js/earnings-verification.js`, `templates/wf_dashboard_base.html`

## 18:32 — Fleet test suite back to green + 3 real bugs it was hiding
The 9 long-standing `fleet.tests` failures turned out to be a mix of stale assertions and three genuine bugs. Fixed the bugs: (1) `record_transaction` stamped `wallet_balance_after`/`cod_in_hand_after` before `sync_cod_in_hand()` re-derived them, so every ledger row (COD ledger, PWA transaction detail, print voucher, CSV export, API) printed the balance *before* its own transaction — now re-stamped after the sync; (2) the pickup scanner wrote `in_transit`, which is not a legal driver transition from `assigned`/`accepted`, so the pre_save state-machine guard silently rolled it back while the driver was told "Pickup confirmed!" — production had literally zero tasks in that status, confirming the path never worked; it now goes `assigned → accepted → picked_up` and returns the state-machine reason instead of a false success; (3) `start_ride` had the same silent-success hole and now verifies the transition actually persisted.
Test side: updated the assertions that outlived the G5 derived-balance change (wallet/COD tests now build real collected tasks instead of writing balance fields directly, COD submission posts `delivery_ids` since the view derives the amount server-side), fixed the staff-only `/fleet/` list expectation and the `'Inactive'` vs `'inactive'` vehicle default, and added 3 tests for the newly-closed holes. Suite is 159/159 green, baseline is no longer 9.

**Files:** `fleet/wallet_service.py`, `fleet/views.py`, `delivery/views.py`, `fleet/tests.py`

## 19:05 — Driver payout: COD removed from the money side + per-driver payout worksheet
Saved the build plan first at `.claude/docs/09-workforce-fleet/payout-console-plan.md` (driver + client legs, both shaped verify → pay → invoice, with the gaps found listed as G1–G7). Then built the driver leg.

The Driver Payouts landing was showing COD as earnings: `real_pending_earnings` was `Sum(cod_collected_amount)` over unsettled COD tasks but labelled "Pending driver earnings", so the figure staff worked from was the driver's COD liability. It now sums unsettled `earning`+`bonus` transactions — the same source the payout invoice itemises — and the "Deliveries After Settlement" count, which had been counting every delivered task ever, is scoped to deliveries completed since the last payout and now includes partial deliveries.

New per-driver payout worksheet at `/workforce/fleet/driver-payout/<driver_id>/`: "Ready to pay" (payable earning/bonus/deduction lines with a sticky selection tape and a Create-payout action) above a "Verification queue" (delivered tasks with client charge, COD read-only, activity log, editable earning, bulk set-amount and verify/publish/reject, reusing the existing earnings_verification_action endpoint). The new `driver_payout_create` action settles earning/bonus/deduction only, recomputing totals from `select_for_update` rows, and rejects COD rows outright — so a payout and its invoice can never disagree. It is a separate endpoint from `bulk_settle_transactions` on purpose: that one doubles as the staff COD hand-in, and the COD section is off limits.

Found while testing: production has **zero** earning/bonus transactions and all 1,273 delivered tasks sit at `pending` verification — no driver earning has ever been published, so nothing has ever been genuinely payable. The 3 existing settlements were built from COD rows. Design is teller-tape ledger (`dpw__`) matching the client payout screens.

**Files:** `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/fleet_drivers_earnings.html`, `workforce/templates/workforce/driver_payout_worksheet.html`, `workforce/static/workforce/css/driver_payout_worksheet.css`, `workforce/static/workforce/js/driver-payout-worksheet.js`, `.claude/docs/09-workforce-fleet/payout-console-plan.md`

## 19:55 — Client payout leg: charge verification, Decimal charges, payout+invoice redesign
Built the client side of the payout console in parallel (three agents on disjoint files), completing the plan saved earlier.

**Model (delivery/0040):** added the client mirror of the driver earnings-verification fields — `charge_verification_status`, `verified_delivery_charge`, `charge_verified_by`, `charge_verified_at` — and converted `dl_price` from IntegerField to `DecimalField(10,2)` so the client delivery charge can hold fils (in-place `USING ::numeric` cast, no data loss, 1273 rows). Two side effects worth recording: `dl_price = order.dl_amount` had been silently truncating fils on write and now stores exactly (historic rows keep their truncated values — the precision was lost at write time), and `dl_price` is exposed in the public `DeliveryTaskListSerializer`, where DRF would have started emitting the string `"20.00"` in place of the number `20` for every external consumer; it is now declared with `coerce_to_string=False` and verified to render as `20.0`.

**New Client Charges console** (`/workforce/client-charges/`): the client-side mirror of Earnings Verification — per-task calculated vs verified charge, business/status/period filters, activity log, bulk set-amount and verify/publish/reject. Publishing books no transaction; the charge is only billed when the business payout runs.

**Client payout + invoice redesign:** the payout report was rebuilt in the teller-tape vocabulary shared with the driver worksheet, with the deduction editor promoted out of the confirm modal onto the page as a subtraction ladder so the running net is visible before staff commit. The invoice got a real A4 print path (the base template's flex/overflow wrappers had been clipping the ledger's right edge). Found and fixed: the confirm modal renders outside `.bcp`, so its design tokens were undefined and the receipt had been rendering with no background at all.

**Wiring:** the payout now deducts `verified_delivery_charge` when set, falling back to `dl_price` — resolved identically in four places (report subtotal, the locked-row settle action, the invoice fee column, and the report template's displayed fee and `data-fee`), using `is not None` so a charge deliberately verified at 0.00 is not silently replaced. Verified by flipping one live task's verified charge from 20.00 to 7.25 and confirming the displayed total moved by exactly −12.75, rolled back. Also removed the duplicate "Driver COD Requests" sidebar link, which pointed at the same URL as "COD Settlement Requests", from desktop and mobile. COD collection and submit-to-admin were left untouched throughout, as instructed. Fleet suite 159/159, site 200.

**Files:** `delivery/models.py`, `delivery/migrations/0040_deliverytask_charge_verification_status_and_more.py`, `ezzy_api/serializers.py`, `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/client_charge_verification.html`, `workforce/static/workforce/css/client_charge_verification.css`, `workforce/static/workforce/js/client-charge-verification.js`, `workforce/templates/workforce/cod_business_settlement_report.html`, `workforce/templates/workforce/cod_business_payout_invoice.html`, `workforce/static/workforce/css/cod_business_settlement.css`, `workforce/static/workforce/css/cod_payout_invoice.css`, `workforce/templates/workforce/parts/sidebar/finance.html`, `workforce/templates/workforce/parts/dashboard_sidebar_workforce_mob.html`, `fleet/templates/fleet/parts/driver_earnings.html`

## 20:15 — Fix missing Driver.driver_name (COD settlement was raising AttributeError)
`Driver.driver_name` never existed on the model, but six Python call sites read it — the COD settlement action and its auto-flow context, the driver CSV export, the availability alert, and two API responses — plus several templates. The Python callers raised AttributeError the moment they ran; the templates just rendered blank. The COD settlement one was the damaging case: it sits after `submit_cod_to_admin` inside the atomic block, so settling a driver's COD would blow up after the money had been moved.

Fixed as a model property rather than patching six call sites: Profile name first (that is the name staff maintain), falling back to the auth user's full name, then username, then driver code, then id — always a non-empty string. No migration needed.

Verified by running the real `cod_settlement_action` for driver 153, who is holding 1485 QAR, inside a rolled-back transaction: HTTP 200, `settled_drivers[0].name` = "Hamad Al Farsi", transaction count restored afterwards so no COD was actually settled. The property also resolves for drivers with no Profile row and for Arabic names. Fleet suite 159/159, site 200.

**Files:** `fleet/models.py`

## 21:05 — Gap audit across the payout/COD domain + 4 fixes applied
Ran four parallel read-only audits (money-flow correctness, access control, workflow completeness, data integrity) over the payout console and the surrounding COD/earnings code, verifying the headline claims myself against production data before accepting them. Full findings summarised to the user; the dangerous ones are recorded here.

**Fixed in this pass (4):** (1) `order_autoflow_status` had only `@login_required` — any logged-in driver or business owner could walk sequential order ids and read the rendered WhatsApp bodies containing customer names and phone numbers across every tenant; now `@api_staff_required`, verified a non-staff session gets 403 with no body. (2) `export_orders_csv` had *zero* decorators — the `@login_required`/`@staff_required` pair above it landed on the `csv_columns_response` helper because a comment block sat between them and the def, so a 5000-row cross-tenant PII export was protected only by the accident of that helper being called last; decorators moved onto the view, non-staff now 302s to /dashboard/. (3) The payout landing's "Last Paid" / "Deliveries Since Payout" read a `settlement` DriverTransaction that no payout path writes — my own regression from this session — so it showed "Never" forever and counted every delivery a driver ever made; now reads `DriverSettlement.paid_at`. (4) The Client Charges date column was blank on 100% of rows: `completed_at|date:"…"|default:…|date:"…"` formats first, so the second `date` filter received a string and returned empty.

**Reported, not fixed (need a decision, several are COD-adjacent):** `bulk_settle_transactions` filters only on `settlement__isnull=True` with no `cod_settled` check — 627 already-deposited COD rows worth QAR 194,908 are checkbox-enabled today, and settling them would mint a phantom deposit and overwrite `cod_submission_txn` on the original tasks; it also computes totals before opening its atomic block with no row locking. `recalc_wallet --apply` force-sets `cod_settled=True` to close a gap it derives from the ledger rather than from task truth, which would mark ~6,187 QAR the driver still holds as settled and immediately payable to businesses. The driver-facing earnings POST (`fleet/views.py:1416`) lets a driver zero their own `pending_earnings` without linking the earning rows, so staff would pay again. There is no reversal for a driver payout at all. `get_wallet_status` shows drivers a wallet of QAR 324,302 (sums collections and deposits as positives) against a true −6,695.

**Data state worth acting on:** QAR 208,440 owed to businesses across 652 tasks, oldest 134 days, one payout ever executed; 3 legacy settlements totalling 13,846 booked as owed *to* driver 82 when it was COD they owed Ezzy, double-counted, and rendering differently on two screens; zero earnings ever published against ~9,550 owed; 1,485 held 140 days by a blocked driver.

**Files:** `workforce/views.py`, `workforce/templates/workforce/client_charge_verification.html`

## 22:20 — All pagination moved to the shared brand component + two global fixes
Converted the last 14 hand-rolled pagers to `includes/components/_pagination.html` (three agents on disjoint apps), starting with Fleet Transactions. Sixty templates were already on the component; `seller_warehouse_links_backup.html` was skipped as dead (no view references it).

The recurring trap was that each hand-rolled pager hardcoded its own filters into its hrefs, so swapping in the component silently drops them unless they are moved into `filter_params`. Every converted page now builds `filter_params` from the whole `request.GET` minus page/per_page, which is a superset of what the old markup carried — and that turned up several pre-existing filter-loss bugs: the orders "all list" pager dropped custom date ranges, the whole delivered-date filter set and sort/dir; the pending-orders pager dropped sort entirely; the driver PWA pager dropped `search`; and Fleet Transactions was stripping `view` from filter_params and re-adding it by hand, so the shared component would have bounced staff back to the default COD tab.

Two problems turned out to be global rather than per-page, so they were fixed once in `core/context_processors.pagination_defaults` instead of in ~35 views: `per_page` was missing from 29 views' context, making the page-size selector snap back to 50 after one click; and `filter_params` was referenced by six warehouse/orders templates that no view ever defined, so those pagers dropped every filter on page 2. A view passing its own value still wins, since render() context is applied after context processors.

Also fixed centrally: the component emitted `pgn--compact`/`pgn--minimal` while `pagination.css` styled `.pgn.compact`/`.pgn.minimal`, so the `variant` parameter had never done anything anywhere; and the component linked `pagination.css` with no cache-buster at all. Dead pager CSS was removed across product, delivery, warehouse, business-mobile, workforce and fleet (each class grepped repo-wide first), and a `FieldError` was fixed in `inventory_list` that had left the Category filter dropdown permanently empty.

Known tradeoff: the two product list pagers used htmx (`hx-get` into `#main-content`); the component emits plain links, so page changes there are now full navigations.

**Files:** `core/context_processors.py`, `ezzydelivery/settings.py`, `webpages/static/webpages/css/pagination.css`, `templates/includes/components/_pagination.html`, `templates/{wf,wh,business}_dashboard_base.html`, plus the 14 converted templates and their views/CSS across workforce, fleet, product, delivery, warehouse, orders and business

## 17:36 — COD legacy reconciliation: filter bar (driver, dates, method, business, status, search)
The page listed every affected deposit for all drivers at once — 10 deposits and 423 legs on one screen. Added a filter card at the top: driver, deposit date from/to, payment method, business, order COD status, free-text search (deposit code / task # / order # / customer / reference) and a "legs to list" toggle that defaults to electronic legs only instead of printing all 423. Filters narrow which deposits are listed and which legs print under each, but every money figure is still computed from the deposit's full leg set, since a hand-in only reconciles as a whole; the card shows "N of M deposits · K legs listed" and each deposit shows its own shown/total leg count. Also fixed the Export CSV button, which pointed at the page URL without `?export=csv` and so just reloaded the page — it now exports and honours the active filters.

**Files:** `workforce/views.py`, `workforce/templates/workforce/cod_legacy_reconciliation.html`, `workforce/static/workforce/css/cod_legacy_reconciliation.css`

## 22:55 — Drivers list: flush layout + filter row controls made to agree
Removed the page padding from the drivers list. The rule was shared by `.drl, .dra, .dri, .drp, .sll…`, but the file's own convention is that `.drl` has already had its ledger-console pass and is scoped separately from its sibling tabs until they get theirs — so the padding came off `.drl` alone and everything else was left as it was.

Then squared up the filter row, which had three separate problems. The search input and the multiselect toggle were both a fixed 34px, but the three `<select>`s had no height at all and were sized by their own larger padding, so they stood taller than their neighbours. The export button likewise had no height and its padding plus 1px border left it a couple of pixels short. And the global control rules in `mobile-app.css` apply `appearance: none` to every select **without** putting an arrow back, so Work Preference, Working Hours and Sort By were rendering as plain text boxes with no dropdown affordance at all — the arrow tile is now declared here in full, with explicit repeat/size/position and right padding so a later `background` shorthand cannot wipe it.

Verified in a real browser rather than by reading the stylesheet: logged in with a minted staff session under headless Chromium and measured every control on the row — search, multiselect, three selects, primary button, export — all now 34px tall, same top edge, same 0.4rem radius, arrows present on all three selects.

**Files:** `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 17:58 — Driver payout worksheet redesign: flush ledger console, exception rail, brand palette
Redesigned `/workforce/fleet/driver-payout/<id>/`. The page listed 562 identical-looking deliveries, all at the same 10.00 fee, each in a 53px row with a huge input — so the thing staff are actually hunting (the row that is NOT ordinary) had no visual priority. The page now states the flat rate once in a rail across the top and counts the two ways a row leaves it (priced from the client charge, or adjusted by staff), each clickable as a filter; those rows carry a thin yellow edge on the fee cell and everything else stays silent. All page padding is gone — panels sit flush and are separated by hairlines instead of gaps — and the sage/brass "teller paper" palette was re-anchored on the Brand Kit navy + Ezzy yellow so the page stops clashing with the navy dashboard shell around it.

Density and behaviour: rows dropped 53px → 33px (11 visible instead of 6), every control in a toolbar now shares one 26px height (the global mobile-app rules target `input[type=…]`, which outranks a plain BEM class — all controls are scoped under `.dpw` to win), the queue got its own scrollport so the column header actually pins (a sticky header never fired because the dashboard shell is an overflow container), plus shift-click range select and Enter-to-next-row on the fee inputs. The route column now reads as a Qatar zone pair (`Marvan Store → Z70`) using `order.dl_zone`, which is where the drop zone actually lives — the old cell read the address record, which is empty on every row. Also fixed the fee fallback to use `is not None` so a fee verified at 0.00 is no longer replaced by the calculated value.

**Files:** `workforce/views.py`, `workforce/templates/workforce/driver_payout_worksheet.html`, `workforce/static/workforce/css/driver_payout_worksheet.css`, `workforce/static/workforce/js/driver-payout-worksheet.js`

## 23:30 — Business filters are now searchable Select2 pickers
Every business dropdown on a filter bar is now a Select2 with a search box. Wired once in `main_dashboard_scripts.html` against `select[name="business"], select[name="business_id"]` rather than editing the 15+ templates that carry one, so a new filter bar gets it for free and the behaviour cannot drift between pages. Select2 was already loaded on all four dashboard bases; there was a half-built `select2-enable` convention referenced only in the HTMX re-init and used by no template.

Scoped to selects inside GET forms. That is exactly the filter case, and it avoids the trap on create/edit forms: Select2 hides the original `<select>`, so a `required` field then fails HTML5 validation with "not focusable" because the browser cannot focus a hidden control. Elements can also opt out with `data-no-select2`, and anything already carrying a Select2 instance is left alone so the order-add/edit and product pickers keep their own config.

New `webpages/css/select2-business.css` sizes the picker to the 34px filter controls it sits beside, scoped under `.ez-biz-select` so the project's other Select2 instances keep their own sizing.

Debugging note worth keeping: the first attempt applied no styling at all, and the cause was not obvious from the markup — `containerCssClass` is a Select2 3.x/4.0 option that **4.1 silently ignores**, so the styling hook never landed and the picker rendered at Select2's default size. Traced by wrapping `jQuery.fn.select2` in the browser to capture a stack trace, which showed the call was firing correctly and the option was the problem. The container/dropdown classes are now applied by hand from the instance.

Verified in headless Chromium on three filter pages: picker present, 34px matching its row, search box open and filtering (41 businesses → 27 for "a", → AnfasicOudPerfume for "oud").

**Files:** `templates/includes/main_dashboard_scripts.html`, `webpages/static/webpages/css/select2-business.css`, `templates/{wf,wh,business,fleet}_dashboard_base.html`

## 23:55 — All staff filter selects searchable + fixed the silent HTMX break it caused
Generalised the business-picker work: every filter select on the staff dashboards (`/workforce/`, which includes warehouse) is now a Select2, so a whole filter bar reads the same instead of one enhanced control sitting beside five native ones. The search box appears only from 8 options up — below that Select2 renders the same box with no search field, which avoids a pointless search over four statuses. The pagination component's rows-per-page control is excluded (it belongs to the pager, not the filter bar), as are multi-selects, anything already carrying a Select2 instance, and anything marked `data-no-select2`. Still GET-forms-only, so create/edit forms keep their native controls and their HTML5 validation. `select2-business.css` became `select2-filters.css` with the class renamed `ez-filter-select`.

**The part that mattered:** driving the real Select2 UI in a browser showed the drivers-list job-type filter setting its value to `full_time` and then doing nothing — no request, no reload. Select2 announces a pick with jQuery's `.trigger('change')`, which only runs jQuery-bound handlers and never dispatches a real DOM event; HTMX (`hx-get` on the select) and inline `onchange="this.form.submit()"` both listen natively, so every one of those filters would have looked like it worked — the value visibly updates — while silently never filtering. Bridged by dispatching a native `change` on `select2:select`/`select2:clear`. Re-tested after the fix: the drivers job-type filter now issues `?…&job_type=full_time` and the row count changes, and the temp-orders business filter submits its form to `?…&business=899260`.

Verified across six staff pages (drivers, COD ledger, fleet transactions, client charges, earnings verification, warehouse inventory): every filter select enhanced, no JS errors.

**Files:** `templates/includes/main_dashboard_scripts.html`, `webpages/static/webpages/css/select2-filters.css` (renamed), `templates/{wf,wh,business,fleet}_dashboard_base.html`

## 00:20 — Searchable filter selects extended to the business dashboard
Same treatment as the staff side, now on the business dashboard too. Swapped the URL-path gate for a `data-dashboard` marker on `<body>` (`staff` on the workforce and warehouse bases, `business` on the business base): order, product and delivery pages render inside the business base from their own apps, so a path test would have missed them. The fleet base deliberately carries no marker — it is the driver phone PWA, where a desktop picker with a dropdown search does not belong.

Verified with a real business login: `/business/finance/` renders the marker and both its filter selects are enhanced, and picking a value through the actual Select2 UI submits the form (`?days=7`), so the native-change bridge holds on this side too. The fleet PWA was checked with a driver login at 390px — marker absent, 4 selects, 0 enhanced, still native. No JS errors on either.

Worth noting for future probes: the business base renders mobile and desktop filter bars together, so most `select[name=...]` queries match two elements and half are `display:none` — a test that grabs the first match will click a hidden control and report a false failure.

**Files:** `templates/includes/main_dashboard_scripts.html`, `templates/{wf,wh,business}_dashboard_base.html`

## 18:19 — Drivers list: one post-hero card
Everything below the hero on `/workforce/drivers/` (tally strip, filter bar, roster table) now lives in a single `.wf-content` card instead of three stacked boxes, with 0.375rem padding so the sections line up with the hero above. The strip and filter lost their own borders and read as card sections divided by hairlines, and the hero is no longer fused to the strip — it goes back to the standard rounded navy card used across the staff dashboard. Verified with a headless screenshot on a staff session.

**Files:** `workforce/templates/workforce/drivers_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 18:23 — Sellers list: same one post-hero card
Applied the drivers-list treatment to `/workforce/sellers/`: the tally strip, filter bar and seller table now sit inside a single `.wf-content` card with 0.375rem padding, their own borders reduced to hairline section rules, and the hero unfused back into its standard rounded navy card. Also dropped `.sll` from the list-page wrapper padding (desktop and mobile) so the page runs flush to the frame like `.drl` — the card's own inset is the only gutter now.

**Files:** `workforce/templates/workforce/sellers_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 18:41 — Seller detail: account-standing stamp in the hero
The seller dossier hero now carries a second rubber stamp beside the verification mark showing the business account standing — Active (green), Suspended (red), Awaiting approval (gold), Inactive/Unknown (muted) — tilted the other way so the pair reads as hand-stamped. The old small dot-pill on the identity line was removed since it said the same thing, and its now-dead CSS went with it; the flags line only renders when there is a fulfillment flag to show. Pending standing reads "Awaiting" rather than "Pending" so it can't be mistaken for the gold PENDING verification stamp next to it. Checked active/pending/suspended sellers on desktop and a suspended one at 390px.

**Files:** `workforce/templates/workforce/seller_detail.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 00:35 — Staff sidebar: dropped Pending Sellers (Sellers menu) and COD Collection (Fleet A/Cs)
Removed the "Pending Sellers" entry from the Sellers menu and the "COD Collection" entry from Fleet A/Cs, in the desktop sidebar and the matching mobile one (where the seller entry was labelled "Pending Approval"). The now-unused `{% url %}` assignments were removed with them rather than left dangling.

Left in place deliberately: the Onboarding menu has its own "Pending Sellers" link, which is a different menu and was not part of the request — flagged to the user rather than removed on assumption. The `fleet:cod_collection` page itself is untouched and still reachable from the driver PWA; only the staff shortcut is gone.

Verified by rendering a staff page and asserting the two ids are absent while their neighbours (All Sellers, COD with Fleet) still render.

**Files:** `workforce/templates/workforce/parts/sidebar/sellers.html`, `workforce/templates/workforce/parts/sidebar/fleet_accounts.html`, `workforce/templates/workforce/parts/dashboard_sidebar_workforce_mob.html`

## 00:45 — Renamed "Address Verify Queue" to "MSG Queue"
Renamed the label in all five places it appeared: the page heading on the queue itself, both sidebar entries (Import and Tasks menus, the latter keeping its pending-count badge), and both mobile sidebar entries. Also renamed the "Verify Queue" shortcut row on the Temp Orders page, which links to the same destination — leaving it would have meant two different names for one page in the same navigation.

The hero subtitle ("Customer WhatsApp address verification jobs from auto-import") and the shortcut's tooltip were left as they are: they describe what the queue does, which has not changed.

Verified by rendering the queue page (heading now "MSG Queue"), the Temp Orders page and a staff page carrying the sidebar — zero occurrences of the old label anywhere.

**Files:** `workforce/templates/workforce/temp_verify_queue.html`, `workforce/templates/workforce/temp_orders.html`, `workforce/templates/workforce/parts/sidebar/import.html`, `workforce/templates/workforce/parts/sidebar/tasks.html`, `workforce/templates/workforce/parts/dashboard_sidebar_workforce_mob.html`

## 01:00 — Fleet transactions empty state was lying, and advertised a button that does not exist
`/workforce/fleet/transactions/?driver_id=82&view=cod_submit` showed "This driver has no transactions yet. Click 'Generate Demo Transactions' to create sample data." Two problems: driver 82 has **23** transactions (21 collections, 2 deposits), and there is no demo-data generator anywhere in the codebase — the copy pointed at a button that was never built, in a production finance console.

The cause is that the tabs filter by transaction type while the message spoke about the driver. The Submit-COD tab filters `transaction_type='cod_driver_settle'`, so it renders empty for a driver with hundreds of rows. The empty state is now tab-aware: it names what the tab is looking for, reports how many transactions the driver actually has, and offers a link back to the full COD view so staff are not left at a dead end. Added `driver_txn_total` to the view for that. The genuine no-rows case keeps a plain message, and the no-driver-selected case already had its own block further down, so that branch was dropped as dead code.

Caught while verifying: the explanatory comment I added rendered as visible page text. Django's `{# #}` is single-line only — a multi-line one is not parsed as a comment and leaks into the HTML. Switched to `{% comment %}`.

**Worth flagging separately:** there are **zero** `cod_driver_settle` rows platform-wide, so the "Submit COD to Admin" tab is empty for every driver. Driver submissions go through `submit_cod_to_admin`, which writes a `cod_deposit`, not a `cod_driver_settle` — so either that tab is watching for a row type nothing creates any more, or the approval flow it was built for is dead. Left alone: it sits inside the COD path.

**Files:** `workforce/views.py`, `workforce/templates/workforce/fleet_transactions.html`, `workforce/static/workforce/css/fleet_transactions.css`, `templates/wf_dashboard_base.html`

## 19:01 — Driver payout worksheet: the pay ladder replaces the figures strip
Redesigned `/workforce/fleet/driver-payout/<id>/` again. The head of the page was five loose figures whose lead number was `Net payable 0.00 QAR` — which reads as a bug rather than as "the money has not reached that gate yet". Replaced it with a ladder of the four gates a driver's money actually moves through — awaiting verification → verified, not published → ready to pay → last paid — each showing its own QAR and count, with a yellow marker on the gate holding money right now. For this driver that turns a meaningless `0.00` lead into `5,640.00 QAR awaiting verification, 564 deliveries`. Each gate is also a link into the work it names, so the ladder doubles as the page's navigation; added an `only=verified` filter to match.

The queue totals needed a SQL twin of `DeliveryTask.calculate_driver_earnings` (a Python property) so the page can state what the whole queue is worth without walking 500 rows — a `Case/When` mirroring the same rules, aggregated alongside the existing tallies.

Fixed while in there: the Date column was blank on every row (`|date|default:|date` formats first, so the second filter got a string — `{% firstof … as %}` has the same trap, so it is an explicit `{% if %}`); the Status column carried `width:100%` and so absorbed ~600px of slack, stranding the log button at the far edge (slack moved to Business, and the resulting gutter is now ruled as the divide between "what was delivered" and "what it is worth"); zero charges printed `0.00` on 50 rows and buried the one real figure (now an em dash); the 50 stacked bordered fee inputs were the loudest thing on a page whose point is that all 50 are identical (borderless until row hover, focus or edit — so a bordered fee now always means "this one is different"); COD carried the biggest numbers on screen despite being explicitly not money (sized down, tint dropped); and the empty "Ready to pay" panel plus the masthead's "last paid" were both repeating what the ladder now says, so they are gone. Yellow was being spent in five places — cut to two roles: the gate holding money, and the create-payout action.

**Files:** `workforce/views.py`, `workforce/templates/workforce/driver_payout_worksheet.html`, `workforce/static/workforce/css/driver_payout_worksheet.css`

## 19:05 — Fleet transactions: one console box under the hero
On `/workforce/fleet/transactions/`, everything below the hero — driver profile, view tabs, filters, action bar and the ledger — was five separately-bordered cards floating on a grey field with 1.25rem gaps, inside a page padded 1.5rem all round. They are steps in one task, so they now sit in a single `ftr__console` box divided by hairlines, and the page padding is 0.375rem left/right only. Inner card frames, radii and margins are dropped inside the console; `overflow` stays visible so the filter dropdowns can still escape. Verified on both the empty `cod_submit` tab and the populated `cod` tab.

**Files:** `workforce/templates/workforce/fleet_transactions.html`, `workforce/static/workforce/css/fleet_transactions.css`, `templates/wf_dashboard_base.html`

## 01:20 — Client payout account list showed 8 of 16 active businesses
The Account dropdown on the Business COD Payout page listed only businesses that currently have pending payout candidates — 8 — while 16 businesses are active. Nine active accounts were simply absent, so staff could not tell "this business is owed nothing" apart from "this business is missing from the list", which reads as a broken filter.

The list is now every active business **plus** any inactive one still holding COD. That second half matters: one account holding COD is not `active`, and under the old rule it happened to appear only because it had candidates — under a naive "active only" fix its money would have become unreachable from this screen entirely.

Each option now carries its outstanding figure ("AOP067 - AnfasicOudPerfume — 126,810 QR held" / "DD021 - DoctorDiet — nothing held"), so the dropdown says where the money is instead of making staff select accounts one at a time to find out.

Verified: 17 options (16 active + 1 suspended-with-COD), 8 showing a held amount and 9 showing none; selecting a business with COD still renders its 258 task rows and one without renders an empty list at HTTP 200.

**Files:** `workforce/views.py`, `workforce/templates/workforce/cod_business_settlement_report.html`

## 19:30 — Change Driver popup could only reach 3 of 132 drivers
Reported as "popup for driver change not able to select other". The modal was working — it just had almost nothing in it. `all_drivers` on the fleet-transactions view filtered to `driver_status='approved'`, and only **3 of 132** drivers are approved (120 pending, 7 processing, 2 blocked), so the picker offered Rahees and two men both named Muhammed. Driver 82, whose ledger staff had just been looking at, is blocked and so was not in his own picker.

This is a financial ledger, and the drivers whose COD most needs chasing are exactly the blocked and never-approved ones, so the filter is gone: all drivers now, ordered approved → processing → pending → blocked, with the status appended to the label so staff can see that the ledger they are opening belongs to a blocked driver. 132 options need narrowing to be usable, so the modal gained a type-to-filter box (auto-focused on open) and a live "N drivers match …" count.

Two smaller faults found in the same code: the select had **two** `change` listeners registered (one in `initializeDriverModal`, one further down the file), so every pick fired two navigations; and a backdrop-click handler called `closeDriverChangeModal()`, which is not defined anywhere — every click outside the dialog threw a ReferenceError. Both removed; Bootstrap already closes on backdrop click. Verified in a headless browser: 132 options, blocked driver 82 present, search narrows to 1, exactly one navigation, no console errors.

Also fixed while in the file: `.ftr__filter-row` used `flex: 1 1 150px`, so the last filter on a wrapped row had nothing to share the line with and stretched edge to edge — it is a grid now, so an orphan keeps one column's width.

**Files:** `workforce/views.py`, `workforce/templates/workforce/fleet_transactions.html`, `workforce/static/workforce/css/fleet_transactions.css`, `templates/wf_dashboard_base.html`

## 19:58 — Driver picker: Select2 search was untypable in the modal + list narrowed to active drivers
Three separate faults were stacked behind "not able to select other".

**1. The Select2 search box could not be typed into.** The shared enhancer already turns every dashboard filter select into a searchable Select2, including this one — but Select2 renders its dropdown on `<body>` by default, which puts the search field outside a Bootstrap modal's focus trap. Bootstrap pulled focus straight back to the dialog, so the control looked alive and silently swallowed every keystroke. Proved it in a headless browser: with the dropdown open, `document.activeElement` was `btn-close` and typing "yousuf" left the field empty. Fixed globally in `main_dashboard_scripts.html` by passing `dropdownParent` = the enclosing `.modal`; focus now lands on the search field and typing filters. Also added a `shown.bs.modal` re-enhance pass, because several driver pickers (the bulk-assign modal, for one) are injected via `innerHTML` long after `DOMContentLoaded` and so were never enhanced at all.

**2. The dropdown styling had never applied.** Every rule in `select2-filters.css` was written as `.select2-dropdown.ez-filter-select__menu`, but `inst.$dropdown` is Select2's dropdown *container*, not the `.select2-dropdown` panel inside it — so the class landed on the container and the entire "Dropdown" section matched nothing. Pickers across all four dashboards had been rendering with stock Select2 chrome. Rewritten as descendant selectors, so the brand search field, navy highlight and shadow now actually show. Inside a modal the results are capped at 11rem and the dialog reserves room (`picker-modal__body`), so the open list sits inside the card instead of spilling onto the backdrop — measured to confirm the dropdown bottom now clears the card bottom.

**3. The list itself.** `approved` only was too narrow (3 of 132 drivers — it could not even show the driver whose ledger was open), and all-drivers was too wide (120 of the 132 are unfinished applications from the public sign-up form that have no ledger and never will). Active is now: on the fleet (`approved` or `processing`), plus anyone who has a transaction however they are flagged now — a blocked driver still holding COD is exactly who this screen exists to chase — plus whoever is open on screen. That is 12 drivers, with the status appended to non-approved names. Select2 inside posting forms stays opt-out except for driver pickers that are not `required`, so the edit-screen driver fields get the search too without breaking HTML5 validation on hidden required selects.

**Files:** `workforce/views.py`, `workforce/templates/workforce/fleet_transactions.html`, `templates/wf_dashboard_base.html`, `templates/includes/main_dashboard_scripts.html`, `webpages/static/webpages/css/select2-filters.css`, `templates/fleet_dashboard_base.html`, `templates/wh_dashboard_base.html`, `templates/business_dashboard_base.html`

## 01:55 — Seller Transactions opens on a roster of active-seller cards
The landing state was a bare "SELECT SELLER" dropdown, which told staff nothing — they had to open accounts one at a time to find out who was owed money. It now opens on a card per active seller (16), ordered by outstanding balance so the money leads, with a filter box for name or code. Selecting a seller still swaps to the existing transaction view, and the "Change Seller" dropdown in the header is untouched.

Each card states the account (mono code + name), the figure staff came for (COD held for that seller), and the three legs underneath — with driver / paid out / orders. The signature is a hairline three-segment rail showing where that seller's collected cash currently sits across the legs it moves through: amber with the driver, navy held by Ezzy, grey already paid out. It encodes the COD pipeline this business actually runs rather than decorating the card.

Brand Kit throughout: monochrome ink on white, hairline borders, 0.375rem padding baseline, mono tabular numerals for money, border-only at rest with the shadow arriving on hover. Yellow is spent once, on the section rule. An outstanding balance is stated by a navy left rule and full-strength ink; a nil balance goes grey rather than disappearing, since the account is still live.

Data comes from two grouped queries (COD legs from DeliveryTask, order counts from Order) — 11 queries total on the landing, not one per seller. Rail widths ride on `data-seg` attributes and are applied by `seller-roster.js`, because templates may not carry inline styles.

Two bugs caught while building, both on the path I had not rendered yet: the roster block referenced `seller_id` before it was read, and a local `from delivery import models as delivery_models` inside the new branch made that name local to the entire function, shadowing the module-level import the selected-seller path depends on — so picking a seller 500'd while the landing looked fine. Both fixed; verified landing and selected states, rail rendering, and the filter (typing "oud" narrows 16 cards to 1) in a real browser with no JS errors.

**Files:** `workforce/views.py`, `workforce/templates/workforce/seller_transactions.html`, `workforce/static/workforce/css/seller_roster.css`, `workforce/static/workforce/js/seller-roster.js`

## 20:12 — Driver picker: approved only, and the dialog stops reserving room it isn't using
Narrowed "active" further on request: `processing` is an application still in progress, not a driver on the road, so it no longer appears. The two exceptions stay — anyone holding a ledger, whatever their flag now, and whoever is open on screen — so the blocked drivers who still owe COD remain reachable. The picker is 5 entries: 3 approved plus MSKP (153) and Yousuf Ahmed (82), both blocked and both carrying transactions.

Also stopped `picker-modal__body` holding its 20.5rem reserve permanently — a dialog containing one select stood 20rem tall with nothing in it. The enhancer now toggles a `--open` modifier on select2 open/close, so the room appears only while the list is showing, with the transition disabled under reduced-motion.

**Files:** `workforce/views.py`, `webpages/static/webpages/css/select2-filters.css`, `templates/includes/main_dashboard_scripts.html`, `templates/wf_dashboard_base.html`, `templates/fleet_dashboard_base.html`, `templates/wh_dashboard_base.html`, `templates/business_dashboard_base.html`

## 02:05 — Bigger seller name on the roster cards
Raised the business name to 0.9375rem/600 in full-strength ink and stacked it under the code instead of sitting beside it. Inline at the larger size, longer names like "AnfasicOudPerfume" would have ellipsised away — and the name is the one thing on the card that has to stay readable, so the code drops to a small grey kicker above it and the name gets a 2-line clamp rather than truncation. Verified in the browser: every current name renders in full, no JS errors.

**Files:** `workforce/static/workforce/css/seller_roster.css`, `workforce/templates/workforce/seller_transactions.html`
