# Dev Log — 2026-07-23

## 03:44 — Remove WhatsApp icons from Zella partner cards on 3PL pricing
Removed the `fa-whatsapp` icon from the WhatsApp buttons on the four Zella Qatar "Digital Partner" cards (E-commerce Automation, Shopify Store Build, Performance Marketing, Store Management) on /3pl/pricing/. The buttons still read "WhatsApp" and link to wa.me — only the icon was dropped. The three EzzyDelivery pricing cards higher on the page keep their icons.

**Files:** `webpages/templates/webpages/3pl_pricing.html`

## 03:45 — Remove WhatsApp buttons entirely from Zella partner cards
Follow-up: removed the whole WhatsApp button (wa.me link) from the four Zella Qatar "Digital Partner" cards on /3pl/pricing/, leaving only the "View Service" button in each card's action row. The EzzyDelivery pricing cards at the top of the page keep their WhatsApp buttons.

**Files:** `webpages/templates/webpages/3pl_pricing.html`

## 04:17 — Zella Qatar brand coloring on Digital Partner section
Recolored the Digital Partner section on /3pl/pricing/ from generic slate grey (#657A89) to Zella Qatar's actual brand teal (#068093, hover cyan #00b8d4, pulled from zellaqatar.com): section background/border, badge, brand name, card top accent bars, icons, feature checks, and Custom Quote pills. The four "View Service" buttons switched from navy to a teal wpp__btn--partner solid so the cards carry Zella's identity instead of EzzyDelivery navy. Bumped pricing.css cache-buster to ?v=20260723a.

**Files:** `webpages/static/webpages/css/pricing.css`, `webpages/templates/webpages/3pl_pricing.html`

## 04:19 — Zella Qatar logo on Digital Partner section
Downloaded the official Zella Qatar line logo (transparent PNG with built-in "Digital Partner" tagline) from zellaqatar.com and placed it at the top of the Digital Partner section head on /3pl/pricing/, replacing the old handshake "Digital Partner" pill badge (its now-unused CSS was removed). Logo is centered at 50px height above the section title. Cache-buster bumped to ?v=20260723b.

**Files:** `webpages/static/webpages/img/zella-logo-line-w.png`, `webpages/templates/webpages/3pl_pricing.html`, `webpages/static/webpages/css/pricing.css`

## 04:26 — New hook copy for Digital Partner section
Replaced the Digital Partner section description on /3pl/pricing/ with punchier hook copy chosen by the user: "ZellaQatar runs your online business — store, marketing, management. EzzyDelivery moves it — pickup, delivery, COD." Old copy was the softer "Our sister company handles the digital side…" line.

**Files:** `webpages/templates/webpages/3pl_pricing.html`

## 06:28 — Fix 3 broken staff-dashboard filters (from filter audit)
Full audit of ~45 workforce pages found 3 broken filters; all fixed. (1) "DL TASK STATUS" dropdown was dead on Incompleted List, Fulfilled Clients Tasks, and Non-Fulfilled Clients Tasks — views read `dmsStatus` (which the shared filter bar never sends) and never applied it; now they read `dlStatus` (with `dmsStatus` as legacy alias) and filter `dl_task_status`, with selected-state echoed back. (2) Dispatch → All Batches "Zone" input was inert — view now filters `destination_zone`. (3) Inventory Reports / Restock List were stub views rendering an always-empty template; implemented seller cards with product counts, `?seller_id=` drill-down to per-seller inventory, and low-stock (≤5) restock list with suggested quantities. All verified via Django test client as staff (filtered rows match expected statuses; regression checks on DL List All and Orders lists pass).

**Files:** `workforce/views.py`, `workforce/dispatch_views.py`

## 06:33 — Fix 4 minor staff-dashboard filter issues (from filter audit)
(1) Fleet Transactions / Seller Transactions: switching driver/seller from the top dropdown navigated with only `?driver_id=`/`?seller_id=`, wiping all other active filters — added an `ftrGo()` helper that preserves the current query string (and resets page). (2) Temp Verify Queue: the failed-rows JS fetch sends `page_size=500` which the view ignored — now honored, capped at 500. (3) Orders lists: a bare `?datePreset=today|yesterday|3days|week|month` did nothing server-side (JS normally fills dateFrom/dateTo) — `all_orders` now resolves named presets to real date ranges when dates are absent. (4) CRM Leads list: Reset link stayed hidden when only the Category filter was active — added `category_filter` to the visibility condition. All verified via Django test client.

**Files:** `workforce/views.py`, `workforce/templates/workforce/fleet_transactions.html`, `workforce/templates/workforce/seller_transactions.html`, `workforce/templates/workforce/crm/leads_list.html`

## 06:54 — Filter audit cleanup: delete dead templates + min/max amount on Fleet Transactions
Deleted the three orphan templates never rendered by any view (`dl_list_incompleted.html`, `orders_list_incompleted.html`, `parts/lists/dl_list_incompleted.html` — the Incompleted route actually renders `dl_list_all.html`); user approved discarding the uncommitted local edits on the parts one. Added Min/Max Amount (QAR) inputs to the Fleet Transactions filter bar — the view already read and applied `min_amount`/`max_amount` but no controls existed. Verified: values filter correctly (all rows in range) and echo back; both affected routes still return 200.

**Files:** `workforce/templates/workforce/fleet_transactions.html`, deleted: `workforce/templates/workforce/dl_list_incompleted.html`, `workforce/templates/workforce/orders_list_incompleted.html`, `workforce/templates/workforce/parts/lists/dl_list_incompleted.html`

## 08:05 — COD reconciliation for task BMS045-2044-AB162 (data fix, no code change)
Investigated BambySilk order #2044: imported from OneDrive with COD 210, but the order's COD was zeroed (untracked staff edit) before the Jul 20 delivery, so the driver app recorded "COD 0 via cash" and no ledger entry was created; COD restored to 210 on the order Jul 22. After user confirmed the 210 stands, recorded the missing collection via WalletService: task marked cod_collected 210 (collected_at = original delivery time), created DriverTransaction CODC-20260723-0001 (cod_collection, cash, note about late reconciliation), order cod_status_by_staff=cod_with_driver, synced driver 61 cod_in_hand 6540→6750. Verified visible in COD Ledger, Fleet Transactions, and COD-in-Hand pages. Note: delivery had a proximity flag (completed 8.3 km from address); earnings for the task remain pending verification.

**Files:** none (production data reconciliation via shell)

## 08:59 — Fix silent COD zeroing on Order Edit / Task Edit (root cause of #2044)
Root cause of the BMS045-2044-AB162 COD wipe: OneDrive-imported orders store cod_status_by_client=None; the Order Edit COD Status dropdown had no blank option, so the browser displayed the first choice ("Online Paid") as selected, and any save then hit the view rule "online_paid → force cod_amount=0" — silently wiping COD on orders staff edited for unrelated reasons (e.g. address fixes). Fixes: (1) order_edit.html now renders a "— Not set —" placeholder selected when the status is empty (empty post = no change); (2) order_edit view forces COD to zero only when the status actively CHANGES to online_paid; (3) both order_edit and delivery_task_edit now leave cod_amount untouched when the field posts blank — staff must type 0 to clear. Verified: edit page for an unset order (#2043) shows the placeholder selected and online_paid unselected.

**Files:** `workforce/templates/workforce/order_edit.html`, `workforce/views.py`

## 09:18 — Friendly CSRF failure page (login "Page Expired" UX)
Investigated ezzyadmin's CSRF error on sign-in: gunicorn journal showed "CSRF token from POST incorrect" on /accounts/login/ — a stale login tab submitting after the token rotated (user was already logged in in another tab). Config was fine, so instead of Django's raw 403 we now have a custom CSRF_FAILURE_VIEW: /accounts/ pages redirect back with an "expired — try again" message (or straight to the dashboard if already authenticated), AJAX gets a JSON 403, and other pages render a branded "Page Expired" card with a same-origin Go Back link.

**Files:** `core/views_csrf.py`, `core/templates/core/csrf_failure.html`, `ezzydelivery/settings.py`

## 10:01 — Temp Orders card green at zero on workforce dashboard
The "Temp Orders (New)" status card (and its quick-action twin) on /workforce/dashboard/ now uses the success (green) modifier when the count is zero instead of primary, matching the existing pattern on the "Pending Publish to Task" card. Still warning (amber) when new temp orders are waiting.

**Files:** `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:02 — Remove Total Orders card from workforce dashboard
Dropped the "Total Orders" status card from the Order Status list on /workforce/dashboard/; the list now starts with Temp Orders (New). View context left untouched (the count comes from the same aggregate query at no extra cost).

**Files:** `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:04 — Replace Published card with Ready to Pickup, first in list
On /workforce/dashboard/ the "Published" status card (task_created count) is replaced by a "Ready to Pickup" card counting orders with order_status='ready_to_pickup', shown first in the Order Status list. It links to All Orders pre-filtered via ?cStatus=ready_to_pickup, amber when non-zero and green at zero. View aggregate swapped published→ready_to_pickup in the same single query.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:09 — Pickup Pool (Active) card on workforce dashboard
Added a "Pickup Pool (Active)" status card right after "Ready to Pickup" on /workforce/dashboard/. Counts first-mile PickupTasks in active statuses (pending/accepted/in_progress/arrived/collected — same definition as the pickups page) and links to the staff pickup pool pre-filtered to active. Amber when non-zero, green at zero.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:15 — FollowUp list: 30-day default window, failed tasks excluded
/workforce/tasks/followup-list/ now defaults its date range to the last 30 days (was 7) and no longer includes tasks with dl_task_status='failed' — 'failed' is removed from the followup status set, so it's gone from both the list and the status filter dropdown. The dashboard "Follow Up Required" count was updated to match (30 days, no failed) so the card and the list stay consistent.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/lists/dl_list_followup.html`

## 10:24 — Location Reconfirm card: no-zone + low-accuracy breakdown
The "Location Reconfirm" card on /workforce/dashboard/ now shows a bracket breakdown next to the label: orders in the reconfirm queue (verification_status pending/needs_review) that have no dl_zone, and those with weak coords_accuracy (landmark, zone_center, or empty). Both counts computed in the same single aggregate query. Current production values: 570 total, 51 no zone, 101 low accuracy.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:25 — No-zone count also catches dl_zone=0 placeholders
Follow-up fix: order EDN132-JO085-AB219 (in the followup list) has dl_zone=0 rather than NULL, so the new Location Reconfirm no-zone bracket missed it. The condition now counts dl_zone NULL or 0 as "no zone" — count went from 51 to 91.

**Files:** `workforce/views.py`

## 10:28 — Location Reconfirm counts scoped to last 30 days
All three Location Reconfirm figures (main count, No Zone, Low Accuracy) now only look at orders from the last 30 days (order_date) instead of the full order history. Production values after scoping: 311 total, 54 no zone, 56 low accuracy.

**Files:** `workforce/views.py`

## 10:30 — No Zone / Low Accuracy bracket moved to Follow Up Required card
Per correction, the No Zone (dl_zone NULL or 0) and Low Accuracy (landmark/zone_center/empty) counts are now computed from the FollowUp task list population (active businesses, followup statuses excl. failed, last 30 days) instead of the verification queue, and shown in brackets on the "Follow Up Required" dashboard card. Location Reconfirm card reverted to its original simple all-time count. Production values: 69 followup tasks, 21 no zone, 8 low accuracy.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:33 — Location Reconfirm card replaced by Location Issues card
Removed the Location Reconfirm card (verification pending count) from /workforce/dashboard/. Its slot now holds a "Location Issues" card fed by the FollowUp task list population: value = distinct followup tasks with no zone OR low accuracy, bracket shows the No Zone / Low Accuracy split, links to the followup list. Follow Up Required card back to a plain label. loc_reconfirm removed from view aggregate and context.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:35 — Location Issues card: counts moved to their own line
Dropped the bracket from the Location Issues card label; the No Zone / Low Accuracy split now renders on its own line beneath the main count (the card content is a flex column, so a third label span stacks naturally — no new CSS).

**Files:** `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:36 — Location Issues card widened to all tasks (last 30 days)
The Location Issues card now counts across ALL delivery tasks from the last 30 days (any status, any business) instead of only the followup list population. Context vars renamed followup_* → loc_* to match. Production values: 127 with issues (56 no zone, 101 low accuracy). Follow Up Required card count unchanged.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:40 — Zone + Accuracy filters on All Tasks list
Added two filter dropdowns to /workforce/tasks/dl_list_all/: ZONE (All / No Zone [dl_zone NULL or 0] / Has Zone) and ACCURACY (All / Low / Not Set / each coords_accuracy level). Wired through view filtering, pagination filter_params, and the filters context; fields gated to list_type == 'all' since the template is shared with the fulfilled/non-fulfilled list views whose views don't handle the params. Verified counts: 1158 total, 99 no-zone, 446 low accuracy.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/lists/dl_list_all.html`

## 10:41 — Location Issues card links to No Zone filter
The dashboard Location Issues card now opens /workforce/tasks/dl_list_all/?zone=missing (All Tasks pre-filtered to No Zone) instead of the FollowUp list.

**Files:** `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:49 — Remove FollowUp List sidebar badge
Removed the count bubble from the "FollowUp List" workforce sidebar link (it counted only pending tasks and never matched the list). Dropped the now-unused followup_count from the workforce sidebar context processor. The business dashboard's separate followup_count stat is untouched.

**Files:** `workforce/templates/workforce/parts/sidebar/tasks.html`, `workforce/context_processors.py`

## 10:53 — Rename Order Status card to ToDo Status
Renamed the "Order Status" card header to "ToDo Status" on /workforce/dashboard/.

**Files:** `workforce/templates/workforce/parts/wf_dashboard.html`

## 10:59 — Fleet transactions status filter matches displayed status
The Settled/Pending filter on /workforce/fleet/transactions/ only checked the settlement FK, which is always NULL for COD rows — so ?status=pending returned every COD transaction including ones displayed as "Settled" (is_received+is_verified, own txn or via the task's cod_submission_txn). The filter now uses the same logic the table renders with: settled = (is_received AND is_verified) OR submission-txn received+verified OR settlement FK set OR type='settlement'; pending = the complement. Driver 61: old pending 505/505, new 184 pending / 321 settled.

**Files:** `workforce/views.py`

## 11:03 — Revert fleet transactions status filter change
Per user request, reverted the Settled/Pending filter on /workforce/fleet/transactions/ back to the original settlement-FK check (settled = settlement set, pending = settlement null).

**Files:** `workforce/views.py`

## 11:11 — COD pending-badge cleanup for driver 61 + badge fix
Two-part fix for stale "Pending" COD rows on /workforce/fleet/transactions/: (1) data — marked driver 61's four legacy deposits (49,640 / 570 / 450 / 160 QAR, ids 560/363/360/358) as received+verified, clearing 128 wrongly-pending collections; (2) template — the COD Collection status badge now also shows "Settled" when the delivery task's cod_settled flag is set, covering old-flow settlements that never recorded a cod_submission_txn link (11 rows, e.g. CODC-20260708-0008). Filter logic untouched per earlier revert. Driver 61 now shows exactly 41 pending, all genuinely unsettled.

**Files:** `workforce/templates/workforce/fleet_transactions.html` (+ data update via shell)
