# Dev Log — 2026-08-07

## 12:22 — Fix drivers CSV export: dropped filters, Excel column split, row selection
The drivers roster export was building its URL with `searchParams.set()`, so any filter picked more than once (status, job type, vehicle, zone, language, availability, hours, verification) collapsed to its last value — `?status=pending&status=processing` exported only "processing", which for the reported URL meant an empty file. Now the key is cleared once and every value appended, verified against the DB: 0 rows before, 88 after. Also fixed the CSV opening as one column in Excel — non-US locales (Arabic/Qatar) split on `;`, so the file now leads with a UTF-8 BOM plus a `sep=,` hint line, which fixes every list-page export at once since they share the helper. Finally added per-row selection checkboxes to the roster (master box in the header, "N selected" pill by the export button, scope line in the modal); ticking rows sends `?ids=` and exports just those, and the row-checkbox support is generic so other list pages can opt in with the same data attributes.

**Files:** `workforce/views.py`, `workforce/static/workforce/js/export-columns.js`, `workforce/templates/workforce/drivers_list.html`, `workforce/templates/workforce/parts/components/_export_columns_modal.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 12:41 — Wire the orders list selection into its CSV export
Extended the selected-rows export to the orders list. That page already had row checkboxes driving the bulk status/AI toolbar, so rather than adding a second checkbox column the existing ones now also carry `data-export-row` (and the header box `data-export-row-all`), letting the shared export JS read the same ticks. Added an "Export CSV" button to the selection toolbar so the option is visible once rows are picked, and made `export_orders_csv` pass `id_field='pk'` explicitly. Verified: full export 1416 rows, three ticked orders export exactly those three with matching order numbers; the scoped lists that hide the export button are unaffected.

**Files:** `workforce/views.py`, `workforce/templates/workforce/parts/lists/orders_list_view.html`

## 12:38 — "Export all N matching this filter" beside the export button
Ticking the header checkbox only ever covered the 50 rows on screen, so there was no way to export a whole filtered set without paging through it. Added a shared `_export_selectall.html` control that sits next to the export button on both the orders and drivers lists: it stays hidden until every row on the page is ticked, then offers "Export all 1,415 matching", and once taken shows a pill with an × to drop back to the ticked rows. Deliberately worded as export-only and reset on any manual tick or HTMX swap, because the orders list shares those checkboxes with bulk status changes, which must never silently widen from 50 rows to 1,415. Where the export caps out the button says so ("first 5,000") rather than truncating quietly. Driven in a real browser through all six states plus four downloads — no JS errors, and the colours inherit so it reads on both the orders list's dark toolbar and the drivers list's white filter bar.

**Files:** `workforce/templates/workforce/parts/components/_export_selectall.html`, `workforce/static/workforce/js/export-columns.js`, `workforce/static/workforce/css/workforce.css`, `workforce/templates/workforce/drivers_list.html`, `workforce/templates/workforce/parts/lists/orders_list_view.html`, `templates/wf_dashboard_base.html`

## 12:47 — Make the orders CSV export match the page it was launched from
The new "Export all N matching" label quotes the page's own count, which exposed that `export_orders_csv` re-implemented the All Orders filter bar and had drifted from it: it filtered on `order_date` where the page filters `created_at`, and ignored `datePreset` entirely, so `?datePreset=month` showed 409 orders on screen but exported all 1,415. Extracted the filter block out of `all_orders` into a shared `_apply_all_orders_filters()` used by both — the same fix already applied to the driver roster for the same reason — and had the export honour the page's sort too. Verified page count == exported rows across six filter combinations including the preset cases.

**Files:** `workforce/views.py`

## 12:55 — Row selection + CSV export on the driver verification queue
The review queue at /workforce/verification/drivers/ had no export at all, so it got the whole set: a checkbox per case, a master box in the manifest header, the "N selected" pill, the "Export all 206 matching" option and a new 20-column CSV built for reviewers (verification state, applied/verified stamps, who verified, rejection reason, account status, vehicle, zones, docs, signup source). It needed its own endpoint because this page spends `status` on verification state and carries the account status on `account`, so the roster export would have read the query string wrong. Extracted `_driver_verification_queue()` so the page and the export share one filter implementation rather than drifting apart the way the orders export had. The per-case checkbox lives inside a `<summary>`, so it stops click propagation — cancelling the event instead would have undone the tick.

**Files:** `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/driver_verification_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 16:17 — Fix driver-verification CSV export blocked by department gate
The export button on /workforce/verification/drivers/ was rejected for every non-superadmin staff member, including ezzystaff (ops+fin+mkt). Cause: `export_driver_verification_csv` was the only workforce URL name missing from the department map in core/departments.py, and unmapped routes fail closed. Added it to the Operations group alongside `driver_verification_list` and recorded it in the staff URL doc. 65 department tests pass; server reloaded.

**Files:** `core/departments.py`, `.claude/docs/09-workforce-fleet/staff-url-departments.md`

## 13:56 — WhatsApp Instances: separate Evolution / WAHA tables + WAHA session sync and delete
The page only ever synced Evolution — a session linked in the WAHA dashboard stayed invisible here until someone hand-typed its name into an instance, which is why it looked like it "wasn't syncing with WAHA". Split the single two-lane table into two separate tables (stored Evolution instances, then live WAHA sessions), added an auto-link that matches a live session to an instance by phone number without ever overwriting a hand-set mapping, and gave each session real controls: start, restart, stop, logout, delete, plus a "New session" creator and a per-row picker to link/unlink a session to an instance. Session control lives in `whatsapp/sessions.py` and validates names strictly instead of falling back to the default session, since a malformed name on a delete would have destroyed the default. The default session's delete button is disabled — every unrouted send falls back to it — and deleting any other session clears the instance still pointing at it. Verified against the live container end to end (created and deleted a throwaway session); 56 whatsapp tests pass.

**Files:** `whatsapp/sessions.py`, `whatsapp/tests_sessions.py`, `workforce/views.py`, `workforce/templates/workforce/whatsapp_instances_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 14:03 — CRM board columns are now staff-managed (add your own kanban stage)
Adding a column to the driver leads board used to need a code edit plus a migration: the funnel was two Python constants in `workforce/crm_views.py` re-labelling the seven fixed `Lead.stage` values, all of which were already spoken for. Introduced a `crm.LeadStage` model — one row per board+column carrying label, order, dot colour, terminal-ness, how long a closed card lingers, and which applicant conditions auto-file a card there — plus a new `/workforce/crm/stages/` page where ops add, rename, recolour, reorder, hide and delete columns per board. A column's conditions come from a fixed picklist (verification status, driver status, uploads-complete, has-deliveries, no-driver-record), evaluated right-to-left so outcome columns beat progress ones; a column with no conditions is a manual lane that reconcile leaves alone, which also fixes drags that used to silently revert. A seed migration reproduces the previous 14 columns exactly — a parity check over all 209 drivers showed zero drift, and both boards render identical counts to before.

Along the way: `set_lead_stage` now validates against the lead's own board (a business lead can no longer be posted into a driver-only stage) and reads `crm_status` from the column instead of a dict that raised `KeyError` for any unmapped stage; per-column `hide_after_days` replaces the global 30-day constant; leads stranded by a deleted column surface in an "Unsorted" lane rather than vanishing; list and detail pages now show the board-correct stage name, so a driver lead reads "Approved" instead of "Won". Column chevron/bay styling and dot colours are derived from the data, so a new column styles itself. 34 crm tests pass (25 new).

**Files:** `crm/models.py`, `crm/stage_rules.py`, `crm/services.py`, `crm/admin.py`, `crm/tests.py`, `crm/migrations/0005_leadstage.py`, `crm/migrations/0006_seed_lead_stages.py`, `workforce/crm_views.py`, `workforce/urls.py`, `workforce/views.py`, `workforce/context_processors.py`, `core/departments.py`, `workforce/templates/workforce/crm/stages_manage.html`, `workforce/templates/workforce/crm/parts/_stage_fields.html`, `workforce/templates/workforce/crm/leads_board.html`, `workforce/templates/workforce/crm/leads_list.html`, `workforce/templates/workforce/crm/lead_detail.html`, `templates/wfnotes/workforce_crm_leads_list_card_notes.html`, `workforce/static/workforce/css/crm.css`, `workforce/static/workforce/js/crm_stages.js`, `workforce/static/workforce/js/crm_leads_board.js`

## 17:15 — Driver board: stop accepting stage moves that silently revert
Staff reported changing a driver lead's pipeline stage (lead 339, Under Review → Uploads Completed) and seeing it snap back on refresh, on both the detail page and by dragging. Root cause was by-design but dishonest: a driver card's column is recomputed from the driver's real `verification_status` on every render, so a move into a rule-driven column the applicant doesn't satisfy was saved and then overwritten with no message. Added `crm.services.stage_move_conflict()` and wired it into `crm_lead_update_stage`, which now refuses such a move with HTTP 409 and an explanation naming the column the applicant actually belongs in, instead of faking success. Business leads are unaffected — their columns carry no auto-rules, which is why that board always worked.

Also closed three related holes found by the CRM council review: a column with a `write_back` but blank confirm text approved/rejected a real driver with no dialog (now auto-fills confirm text, and the reason prompt no longer sits inside the confirm branch); the lead-detail stage chips posted straight through with no confirmation at all and now honour each column's `confirm_text`/`needs_reason` and surface refusals; and the stage badge built via `innerHTML` from a staff-editable column label is now composed with `textContent` (stored-XSS vector). Fixed a regression from this morning where re-keying `stage_counts` to `(category, stage)` left the CRM reports headline tiles reading it with a bare string — Won/Lost showed 0 and Open showed the full total; now Won 5 / Lost 2 / win-rate 71%. 34 crm tests pass.

**Files:** `crm/services.py`, `workforce/crm_views.py`, `workforce/templates/workforce/crm/lead_detail.html`, `workforce/static/workforce/js/crm_lead_detail.js`, `workforce/static/workforce/js/crm_leads_board.js`

## 17:15 — Marketing auto-triggers: driver onboarding + lead messaging, and per-route channel

The Auto Triggers page had nothing at all on the Marketing tab except four sender routes — every lead and driver-onboarding message the platform sends was hardcoded with no switch and no routing. Added ten marketing rows, each one wired to a real send: the join-form thank-you and the "finish your application" reminder, the 3PL quote thank-you and the sales alert, the daily lead follow-up digest, plus five events flows can be built on (lead created / stage changed / won / lost, driver application submitted). Also registered `business_cod_settled`, which had been firing with no row to switch it off.

Split the sender routing so driver leads and business leads are separate rows — they run on different numbers — and gave every route its own channel choice (Evolution API or WAHA), since the same number is reachable on both stacks by different identifiers. A new `send_routed_message()` resolves both halves at send time, so switching the driver-onboarding route to WAHA actually changes which API carries the join-form thank-you. Two recipient types that had been offered in the flow builder for months but resolved to nothing (`staff_ops`, `staff_all`) now work, joined by `staff_fin`/`staff_mkt` and `context_phone` — "the person this event is about", which is what lets a flow message the lead itself.

On the onboarding and lead pages the WhatsApp buttons were plain `wa.me` deep links, i.e. the staff member's own phone. Added a shared composer (`_wa_send_modal.html`, "Send from EZZY") on the driver, CRM lead and pricing-inquiry pages that sends through the section's route and names the sending number before you type. The old wa.me link is still there next to it. Verified end to end: toggling `lead_stage_changed` off stops a live flow, both channels dispatch to the right transport, and 180 tests pass across core/crm/workforce/whatsapp.

Note for follow-up: the driver application thank-you body is currently switched OFF in AI Config → Messages, so that message is not going out regardless of the new trigger.

**Files:** `core/models.py`, `core/whatsapp_utils.py`, `core/auto_flow_executor.py`, `core/views.py`, `core/departments.py`, `core/migrations/0022_whatsappsenderroute_channel_and_more.py`, `core/migrations/0023_marketing_auto_triggers.py`, `crm/services.py`, `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/auto_triggers_list.html`, `workforce/templates/workforce/parts/components/_wa_send_modal.html`, `workforce/templates/workforce/driver_detail.html`, `workforce/templates/workforce/crm/lead_detail.html`, `workforce/templates/workforce/forms/pricing_inquiry_detail.html`, `workforce/templates/workforce/auto_flow_add.html`, `workforce/static/workforce/css/workforce.css`, `workforce/static/workforce/css/auto-triggers.css`, `templates/wfnotes/workforce_auto_triggers_list_card_notes.html`, `templates/wf_dashboard_base.html`

## 17:25 — Driver board: staff moves always stick (pinning) + "back to review queue" column
Follow-up to the 17:15 refusal: refusing a move was honest but still blocked ops from doing the thing they wanted, so driver cards now behave like business ones. Added `Lead.stage_pinned` / `stage_pinned_at` (migration 0007): a manual move that disagrees with the driver's application status pins the card, `reconcile_driver_leads` and the lead-detail mirror both skip pinned cards, and the response carries a warning so staff know the card has stopped tracking the application. Crucially the pin is decided *after* the write-back, so a column that sets the driver's real status (Approved, Rejected, back-to-queue) needs no pin and keeps auto-filing. A thumbtack shows on pinned cards, and the lead page grows a "Resume auto-filing" bar that says which column the card will jump to before you press it. The earlier HTTP 409 refusal is gone — accept-and-pin supersedes it.

Also configured the "Uploads Completed" column with a write-back of *Pending Verification* (confirm text: "send this driver back to the review queue"), which makes the originally-reported action work: lead 339 now moves Under Review → Uploads Completed, resets driver 493 from `under_review` to `pending`, and survives reconcile. Corrected the board's on-page notes, which still claimed driver drags would not stick. 40 crm tests (6 new) and 65 department tests pass.

**Files:** `crm/models.py`, `crm/services.py`, `crm/migrations/0007_lead_stage_pinned_lead_stage_pinned_at.py`, `crm/tests.py`, `workforce/crm_views.py`, `workforce/urls.py`, `core/departments.py`, `.claude/docs/09-workforce-fleet/staff-url-departments.md`, `workforce/templates/workforce/crm/leads_board.html`, `workforce/templates/workforce/crm/lead_detail.html`, `workforce/static/workforce/js/crm_lead_detail.js`, `workforce/static/workforce/js/crm_leads_board.js`, `workforce/static/workforce/css/crm.css`

## 17:50 — Restore CRM (missing wa_session migration) + close the WhatsApp OTP exposure
Every CRM lead page was returning 500: `Lead.wa_session` had been added to the model with no migration, so `crm_lead.wa_session does not exist` broke any Lead query (public site and the staff dashboard were unaffected). Generated and applied `0008_lead_wa_session` — a plain AddField with a blank default — and confirmed all seven CRM pages back to 200.

Then closed the account-takeover path the council review found. Our own outbound auth messages were being ingested verbatim, so password-reset codes sat readable in `WhatsAppMessage.body` and `raw_payload`, and `crm_wa_chat_preview` took `sender` from the query string with no ownership check — any CRM-capable staff member could request a reset for another user, read the code, and complete it. Two independent layers: `whatsapp/secrets.py` redacts codes at both ingest points (backfill and the outbound webhook log), and a new `crm.services.wa_read_blocked()` refuses to open any conversation whose number belongs to a platform account, applied to the inbox chat preview, the lead conversation panel and the bare-pk media proxy. Added `redact_wa_secrets` to sweep rows stored before the fix — 8 redacted. Getting the pattern right mattered: the first version destroyed "a one-time payment of *QAR 900*" and substituted inside a base64 location payload, so redaction now requires stronger context phrases, skips encoded blobs, and only touches digits near the phrase for third-party messages.

Also made a driver write-back an Operations decision (`crm_lead_update_stage` re-checks the desk before running any column `write_back`) and, because the CRM board routes were Marketing-only, opened them to Operations in `_MULTI` — ops previously could not open the driver board at all while marketing could approve drivers through it. A write-back that reaches no driver now says so instead of reporting a silent success (13 driver leads match no Driver row). 49 crm tests (9 new) and 65 department tests pass.

**Files:** `whatsapp/secrets.py`, `whatsapp/management/commands/redact_wa_secrets.py`, `whatsapp/management/commands/backfill_waha.py`, `whatsapp/waha_views.py`, `crm/services.py`, `crm/migrations/0008_lead_wa_session.py`, `crm/tests.py`, `workforce/crm_views.py`, `core/departments.py`

## 17:57 — Lead detail: pick which WhatsApp number the chat runs on

The lead conversation panel merged every WAHA session into one thread and gave staff no say in it, while the "Send from EZZY" composer was locked to the board's routed number — so there was no way to work a driver on Ezzy6000 while business leads sit on the house line. Added a number picker above the chat (hairline tabs, one per linked number plus "All numbers", each showing its message count) that filters the thread and is remembered per lead on the new `Lead.wa_session`. An untouched lead lands on 66451589, unless that number holds no messages for the lead and another one does — landing on a blank panel while the real thread sits one tab away reads as "this lead never replied".

Picking a number also pins the composer to it: `whatsapp_send_routed` now takes an optional `session`, checked against the numbers we actually own (live WAHA sessions + active `WhatsAppInstance` rows) rather than passed through `sessions.normalize`, which would quietly turn a typo into the default session and send from the wrong line. Only an explicit pick overrides the send — merely landing on a tab keeps the Auto Triggers route, so no client sees a different sender without someone choosing it. 11 new crm tests; 116 crm+whatsapp tests pass.

Worth knowing: `WhatsAppMessage` holds 6,943 rows and every one is on `default` — Ezzy6000 and FleetAdmin4545 have no stored history yet, so those tabs read empty until `backfill_waha --session <name>` is run for them.

**Files:** `crm/models.py`, `workforce/crm_views.py`, `workforce/views.py`, `core/whatsapp_utils.py`, `workforce/templates/workforce/crm/lead_detail.html`, `workforce/templates/workforce/parts/components/_wa_send_modal.html`, `workforce/static/workforce/css/crm.css`, `crm/tests.py`

## 18:04 — WhatsApp Instances: drop the pager off the Evolution table

The Evolution instances table was paginated at 25 rows, which only ever rendered a pager bar under a three-row table. Removed the `paginate_queryset` call and the `_pagination.html` include — the WAHA table below cross-references every instance by name, so a second page would have hidden the instance a session claims to be linked to.

**Files:** `workforce/views.py`, `workforce/templates/workforce/whatsapp_instances_list.html`

## 18:11 — Driver identity becomes a real FK; stage-config guards; per-board reports funnel
Cleared the rest of the CRM council review. The big one: driver leads were matched to applicants by the last 8 digits of a phone string with no FK, and the board and the detail page used *different* resolvers (first-key-match vs newest-updated), so one card flip-flopped between columns and a rejection could have been fired at the wrong person. Added `Lead.driver` (migration 0009) as the authoritative link — phone matching now only *finds* a driver once, then the FK is what the board, the detail page and the verification write-back all read. `_driver_for_lead` refuses to guess when a number matches more than one applicant, and `_driver_match_keys` ignores 15-digit WhatsApp LIDs, which previously contributed a fabricated 8-digit key. Reconcile now claims unbound leads per driver instead of sharing one, so every applicant gets its own card: 210 leads bound, and driver 359 — a complete application that had been invisible to recruitment because it shared lead 147 with driver 223 — now has its own card (lead 348). Zero drivers without a card.

Stage config hardening: unticking the sole catch-all column is refused (an unmatched driver would otherwise be filed at whatever column happened to be leftmost), and a new non-outcome column is inserted *before* the first outcome column rather than at the far right, because rules are matched right-to-left and the far right is maximum precedence — staff get told where it landed. `closed_stage_keys()` is now passed a category wherever the queryset is board-scoped, so one board's terminal column cannot reclassify the other's leads. The reports funnel is split per board: it had been merging both column sets, adding "Quoted" and "Uploads Completed" together under whichever label came first. 68 crm tests (19 new, including regressions pinning the duplicate-registration case and the LID key) and 65 department tests pass.

**Files:** `crm/models.py`, `crm/services.py`, `crm/migrations/0009_lead_driver.py`, `crm/tests.py`, `workforce/crm_views.py`, `workforce/views.py`, `workforce/templates/workforce/crm/crm_reports.html`, `workforce/static/workforce/css/crm.css`

## 18:14 — WhatsApp Instances: instance name leads the row, Default becomes a sub-line

Reordered the Evolution table so Instance Name is the first column (Label and Phone Number follow), and dropped the Default column entirely — only one row ever carries it, so the column spent its whole width rendering dashes. "Default sender" now sits as a sub-line under the instance name, matching how the WAHA table below already marks its default session.

**Files:** `workforce/templates/workforce/whatsapp_instances_list.html`, `workforce/static/workforce/css/workforce.css`

## 18:29 — WAHA webhook was signing nothing: every inbound WhatsApp message dropped since 18 July

The CRM inbox showing "no unknown senders" for Ezzy6000 turned out to be the visible edge of a full inbound outage. The compose file set `WHATSAPP_HOOK_HMAC`, but WAHA only reads `WHATSAPP_HOOK_HMAC_KEY` — with no key it sent no `X-Webhook-Hmac` header at all, so `whatsapp/auth.py` 403'd every delivery. 2,214 rejected deliveries were sitting in the container log, across all three numbers; the only rows still reaching the DB came from the nightly backfill, which is why nothing looked broken. Fixed the variable name, recreated the container from compose (the live one had been hand-rolled and drifted: `NODE_OPTIONS` 16384 vs 4096, no mem limit), and live ingestion is back with sub-second lag. The image pin also had to change — `devlikeapro/waha:2026.7.1` is not a published tag, so every `compose up` failed to resolve it; now pinned by the digest of the running, verified 2026.7.1 build. Sessions restored themselves from the volumes, no QR rescan.

Two follow-on gaps closed: Ezzy6000 and FleetAdmin4545 had never been synced (paired after the last cron pass), so contacts were pulled for both — 1,972 and 416 rows — and message history backfilled; the Ezzy6000 tab now lists real unknown senders. And `sync_contacts` was raised from a 60s to a 180s timeout, because the main number's directory is ~6.5 MB and WAHA takes ~80s to serialise it — that call had failed on every nightly pass since 4 August, quietly freezing the default session's contact names.

**Files:** `deploy/waha/docker-compose.yml`, `whatsapp/contacts.py`

## 18:32 — Duplicate leads: two cards in one card
The same prospect arriving twice — a pricing form and a WhatsApp chat, or a driver application and an inbox promote — produced two separate cards. They are now folded into one, without flattening them: `Lead.merged_into` (migration 0010) points an absorbed card at its survivor, and **both rows are kept**. The child keeps its own source, its own inquiry link and its own timeline; it drops off the board and list and renders as a sub-card inside the parent instead, and the parent's card shows both source badges ("WhatsApp Inbound + Pricing Inquiry"). Because nothing is destroyed, a wrong merge is undone with one click.

Auto-merge runs on creation from both public forms: a new pricing-inquiry or WhatsApp-form lead whose number already has an open card on the same board folds into it, with the OLDER card kept as the survivor since staff already know that id. Manual merging lives on the lead page — same-number candidates are listed with a "Merge in" button, and absorbed cards get "Un-merge". Cross-board merges are refused (a business lead and a driver applicant are not the same record) and the tree is kept one level deep, so a card can never hide another card's children. On the driver side reconcile already claimed an existing card by phone rather than creating a second one; that link is now logged on the timeline instead of happening silently, and reconcile-created cards are labelled "Driver Application" rather than the misleading "Manual".

Two things the browser check caught: the sub-card repeated the source badge the parent already showed (now carries the absorbed card's own name), and the manual-column marker was rendering on all seven business headers where every column is manual by definition (now driver-board only). 79 crm tests (11 new) and 65 department tests pass; demo leads used for the screenshots removed.

**Files:** `crm/models.py`, `crm/services.py`, `crm/migrations/0010_lead_merged_at_lead_merged_by_lead_merged_into_and_more.py`, `crm/tests.py`, `workforce/crm_views.py`, `workforce/urls.py`, `core/departments.py`, `.claude/docs/09-workforce-fleet/staff-url-departments.md`, `workforce/templates/workforce/crm/parts/_lead_merge_panel.html`, `workforce/templates/workforce/crm/lead_detail.html`, `workforce/templates/workforce/crm/leads_board.html`, `workforce/static/workforce/js/crm_lead_detail.js`, `workforce/static/workforce/css/crm.css`

## 18:44 — CRM inbox: searching the number on the screen found nothing

Searching `51020251` returned "No unknown senders" even though a row for +97451020251 was sitting on page 1. The inbox stores senders under the id WhatsApp delivered them as — an @lid for nearly everyone — and the search filtered that raw id, while the resolution to a real phone happened afterwards, per row. So the one string a staff member actually has (the number printed on the row) was the one string that could never match. Search now resolves the term through the contact directory (phone, lid, saved/push name) and through the same cached WAHA lid map the rows render from, then matches on the resulting ids in both stored shapes — bare and @-suffixed.

Second defect found while testing it: WhatsApp protocol chatter — encryption notices, hosted-account banners, group membership changes — was being stored as inbound messages. They carry a sender but no body, so each one queued a person for triage who had never written to us; 74 of the inbox's senders were phantoms of this kind, and merely asking WAHA whether a number exists is enough to mint one. The webhook and the backfill now drop these before they become rows (`is_system_event`, single definition in `waha_views`), `call_log` and `revoked` deliberately excluded from that list because a missed call or a deleted message is a real person making contact. The 649 already stored are relabelled `message_type='system'` by migration 0010 rather than deleted — payloads stay auditable — and the inbox excludes them. 141 crm + whatsapp tests pass, 6 new.

**Files:** `workforce/crm_views.py`, `whatsapp/waha_views.py`, `whatsapp/models.py`, `whatsapp/migrations/0010_whatsappmessage_system_type.py`, `whatsapp/management/commands/backfill_waha.py`, `crm/tests.py`

## 18:52 — Ingest redaction was a no-op on raw_payload (found by exercising it)
The OTP redaction added earlier cleaned `WhatsAppMessage.body` but left the code in `raw_payload`, which is just as readable from the CRM chat panels. Cause was argument order in `backfill_waha.upsert_message`: it passed the already-redacted body to `redact_payload`, whose "does this message carry a code?" gate then found no digits and skipped the payload entirely. The unit tests on `redact_text` all passed, and 56 whatsapp tests passed — only calling `upsert_message` with a real auth payload and reading the row back showed it. Fixed by keeping the original body for the payload call, and pinned with two tests that go through `upsert_message` itself rather than the helper. `whatsapp/waha_views.py` never had the bug (it already passed the original text).

Scanned every stored row's `raw_payload` directly rather than trusting the body-based gate: zero rows still hold a code, so the earlier sweep had in fact been correct. Hardened `redact_wa_secrets` anyway so its gate reads the payload as well as the body — a row whose body was cleaned but whose payload was not would otherwise be invisible to the very command meant to find it. 87 crm, 56 whatsapp, 90 core+department tests pass.

**Files:** `whatsapp/management/commands/backfill_waha.py`, `whatsapp/management/commands/redact_wa_secrets.py`, `crm/tests.py`

## 19:00 — CRM toolbar redesign + board lane panning

Rebuilt the shared `crm__toolbar` filter row used by the leads list, board, contacts and WhatsApp inbox. Everything in the row now shares one `--crm-ctl-h` (2.25rem) so search, facets and buttons sit on a single measured baseline (verified at 1440px: seven controls, one line, all 36px); facets got custom chevrons via background longhands, an ink `--on` state so an engaged filter is visible without spending the yellow accent, and the previously dead `is-filled` class now marks a live search term. The toolbar submit moved from yellow to a new `crm__btn--ink` because "New Lead" in the header was already the page's yellow CTA, and Reset became a real bordered control instead of an underlined word. Below 768px the facets become a 2-up grid (1-up under 480px) with 40px touch targets. No filter was removed.

Fixed the reported "can't scroll the board right — right-hand columns unreachable" bug. The lane strip is inside `<main style="overflow:hidden">` and was `min-height: calc(100vh - 210px)`, which put its horizontal scrollbar below the fold with no way to reach it. The strip is now bounded to the viewport by a height that `crm_leads_board.js` measures from its own top offset, and it gained three ways to pan: edge buttons that appear only when the strip overflows (and disable at each end), vertical mouse wheel over the strip, and auto-pan while dragging a card near either edge so a card can reach an off-screen column. Lane side gutters widened to 3.25rem so the pan buttons never sit on top of a card (measured 8px clearance).

**Files:** `workforce/static/workforce/css/crm.css`, `workforce/static/workforce/js/crm_leads_board.js`, `workforce/templates/workforce/crm/leads_list.html`, `workforce/templates/workforce/crm/leads_board.html`, `workforce/templates/workforce/crm/contacts.html`, `workforce/templates/workforce/crm/whatsapp_inbox.html`

## 19:19 — Manual WhatsApp composers: editable starter text + full inventory on Auto Triggers

The "Send from EZZY" button on a CRM lead opened a blank textarea, so every staff member retyped the same intro by hand. Registered four new editable message bodies (`crm_lead_manual`, `crm_driver_lead_manual`, `pricing_inquiry_manual`, `order_verify_manual`) in `core/message_templates.py`; the lead, driver-profile and pricing-inquiry pages now render their starter server-side with the lead's real details filled in and pass it to the shared composer, and all four are editable on the AI Config → Messages tab. `_build_order_whatsapp_message` — the address-verification text used by the order composer, the task composer, the verification queue and the automatic pipeline — now reads its body from `order_verify_manual` instead of a hardcoded f-string; its on/off stays owned by the `wa_location_verification` trigger, so the Messages tab hides the switch there and the save handler forces `is_enabled=True` rather than reading a field the form never renders.

Second half of the task: the Auto Triggers page only showed automatic sends, so half the outbound paths were invisible there. Added a `MANUAL_COMPOSERS` registry and a new "Manual Composers" group in the WhatsApp Notifications card — nine rows covering the three shared-modal pages, the order/task/verification-queue modals, the WAHA agent reply box, and the `wa.me` buttons (explicitly flagged as *not* platform sends, since nothing on that page affects them). Each row cross-links to the sender route that carries it and deep-links to its message body on the Messages tab. Verified end-to-end against the live DB: all pages 200, the composer prefills, a saved edit reaches the real send body, switching a composer template off opens a blank composer without blocking sends, and the order-verification wording is byte-identical to what customers received before. 208 existing tests still pass; added 15 new ones in `core/tests_message_templates.py`.

**Files:** `core/message_templates.py`, `core/tests_message_templates.py`, `workforce/views.py`, `workforce/crm_views.py`, `workforce/templates/workforce/parts/components/_wa_send_modal.html`, `workforce/templates/workforce/crm/lead_detail.html`, `workforce/templates/workforce/driver_detail.html`, `workforce/templates/workforce/forms/pricing_inquiry_detail.html`, `workforce/templates/workforce/auto_triggers_list.html`, `workforce/templates/workforce/ai_config.html`, `workforce/static/workforce/css/auto-triggers.css`, `workforce/static/workforce/css/ai-config.css`

## 19:24 — Pricing inquiries: website / Facebook / Instagram icons per row

Replaced the single "Website" text link in the Business column with a fixed three-slot icon row (globe / Facebook / Instagram) so every row reads the same way at a glance. A channel with nothing openable on file renders as a dimmed, dashed, non-clickable placeholder instead of disappearing, and its tooltip says whether the field is empty or holds text that is not a usable link.

The link building moved into `PricingEnquiry` as an `online_presence` property, because the four stored columns are free text from a public form and hold anything: full URLs, bare handles, business names with spaces, filler like "N/A" and "under development", and Instagram links typed into the website box. The old markup put the raw value straight into an href, so "poolboat.store" resolved against ezzydelivery.qa and "N/A" produced `https://N/A`. Now a value is routed to the channel its host names (a Facebook URL in the website column lights the Facebook icon), a bare handle in a social column becomes a profile URL even when it contains a dot ("poolboat.store" → instagram.com/poolboat.store), and anything with no domain, no valid handle, or whitespace in it stays unlinked with the text kept in the tooltip. The legacy `social_profile` column is folded into whichever channel it matches so older rows stop reading as "no socials", and an unmatched legacy value gets its own link icon rather than being dropped.

Checked against all 123 rows: 89 links produced, none malformed (no spaces, every host has a TLD). Properties only, so `makemigrations --check` reports no changes. The detail page still has its own older inline URL handling and was left alone.

**Files:** `webpages/models.py`, `workforce/templates/workforce/forms/pricing_inquiries_list.html`, `workforce/static/workforce/css/pricing_inquiries_list.css`

## 19:32 — WhatsApp send-surface audit: unauthenticated sender endpoint fixed, task reply box catalogued

Swept every WhatsApp send path in the codebase after the composer work. Found `/workforce/whatsapp/whatsapp_send_message` (`workforce/views.py:26131`) had NO decorators at all — verified by probing it: an ordinary logged-in seller account got a 200 and reached the Evolution API, meaning any non-staff user (seller or driver) could send an arbitrary WhatsApp from the company number to any phone, with CSRF the only barrier. Added `@login_required` + `@api_staff_required` to match its sibling `whatsapp_last_message`, and stopped trusting `instance_name` from the request body — the sender now resolves from the `orders_tasks` route server-side, so a caller can no longer choose which of our numbers a message leaves from. Re-probed after the fix: anonymous 302s to login, non-staff gets 403, staff sends on the routed instance with the client-supplied instance ignored.

Also catalogued the delivery-task page's reply thread as a tenth manual composer — it is a separate widget from the send modal on the same page and was the only caller of that endpoint. Noted on its row that it honours the routed number but always sends over Evolution, so a WAHA channel setting on the Orders & Delivery Tasks route is not respected there yet. Remaining unlisted senders (client dashboard's own per-business WhatsApp triggers, AI agent budget alerts, AI WhatsApp auto-reply) are reported to the user but not wired — they need AutoTriggerConfig rows rather than composer rows.

**Files:** `workforce/views.py`, `core/message_templates.py`
