# Dev Log — 2026-07-22

## 05:19 — Nationality dropdown in driver application
Changed the Nationality field in the public driver application (Section 1, /join_us/driver/) from a free text input to a select dropdown. Added a NATIONALITY_CHOICES list (48 demonyms + "Other") in core/forms.py; DriverApplyProfileForm now builds a form-select widget from it in __init__. Profile.nationlity stays a plain CharField (no migration), and any legacy value already saved on a profile is appended to the choices so it remains selected when editing.

**Files:** `core/forms.py`

## 05:34 — Driver work preference (full/part time + time slabs) end-to-end
Added `job_type` (Full Time / Part Time / Flexible) and `work_time_slabs` (CSV of morning/afternoon/evening/night) to the Driver model with display helpers, plus migration 0029. Driver application Section 2 now collects both (dropdown + checkboxes), saved/validated in core.views.join_driver. Staff review page (view_driver_profile) shows Work preference + Preferred hours with a new vetting-checklist row; driver_detail hero shows the preference; Django admin gains a DriverVacancyAplication registration (previously write-only) and job_type in DriverAdmin. Also removed the dead 'online' availability value from the warehouse dispatch driver filter and excluded the new fields from legacy DriverJoinForm.

**Files:** `fleet/models.py`, `fleet/migrations/0029_driver_job_type_driver_work_time_slabs.py`, `fleet/forms.py`, `fleet/admin.py`, `core/views.py`, `core/templates/core/join_us_driver.html`, `workforce/views.py`, `workforce/templates/workforce/view_driver_profile.html`, `workforce/templates/workforce/driver_detail.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`, `warehouse/views.py`

## 08:45 — Driver availability & work preference: closed all remaining gaps
Wired work preference and availability through the whole stack. Drivers can now edit job type + preferred hours in the PWA Settings (new Work Preference card; also fixed a latent bug where submitting the availability form silently turned off task notifications). Staff can edit work preference inline on driver_detail (new driver_set_work_pref endpoint) and filter the drivers list by work preference and working hours (also shown under the availability badge). API: job_type/work_time_slabs added to driver serializers plus a new POST /api/driver/work-preference/ endpoint. Behavior: new-task WhatsApp broadcasts now skip drivers outside their preferred time slabs (current_work_time_slab helper, blank = always); accept_task auto-flips offline/on-break drivers to available; assignment dropdowns (api_drivers_list + dispatch RiderShiftForm) sort available drivers first and label availability/job type. New hourly cron `expire_stale_availability` sets drivers offline after 12h with no GPS ping/activity/update and no active task (dry-run found 2 genuinely stale drivers). Bonus: fixed dispatch/forms.py which crashed on import (PickupLocation.is_active and RiderShift.STATUS_CHOICES don't exist → pickup_status='active' / SHIFT_STATUS).

**Files:** `fleet/views.py`, `fleet/templates/fleet/driver_settings_pwa.html`, `fleet/models.py`, `fleet/management/commands/expire_stale_availability.py`, `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/driver_detail.html`, `workforce/templates/workforce/drivers_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`, `ezzy_api/serializers.py`, `ezzy_api/views.py`, `ezzy_api/urls.py`, `core/auto_flow_executor.py`, `delivery/views.py`, `dispatch/forms.py` (+ crontab entry)

## 08:58 — Work preference required at driver application submit
Made the Work Preference dropdown mandatory on final submission of the driver application: submit-time guard in join_driver (same pattern as the vehicle-type minimum), label marked with *, and Section 2 completion indicator now also requires job_type. Save Progress still accepts it empty.

**Files:** `core/views.py`, `core/templates/core/join_us_driver.html`

## 09:12 — Installed 6 external frontend design skills
Added supporting design skills from three GitHub repos into .claude/skills/: impeccable (pbakaus — full UI design/audit/polish toolkit with reference docs + scripts), design-taste-frontend (leonxlnx/taste-skill flagship — anti-slop landing pages/redesigns), and four Emil Kowalski skills (emil-design-eng, apple-design, review-animations, animation-vocabulary). frontend-designer.md gained a Supporting Design Skills index with a precedence note (Brand Kit/Bootstrap/BEM rules win on conflict); CLAUDE.md skills table updated.

**Files:** `.claude/skills/impeccable/`, `.claude/skills/design-taste-frontend/`, `.claude/skills/emil-design-eng/`, `.claude/skills/apple-design/`, `.claude/skills/review-animations/`, `.claude/skills/animation-vocabulary/`, `.claude/skills/frontend-designer.md`, `CLAUDE.md`

## 09:28 — Canonical brandkit skill + brand design recipe library
Created .claude/skills/brandkit.md: the full Brand Kit token table (identity colors, ink scale, semantic bg+text pairs, gradients/shadows/radii, type scale) plus 11 brand-supporting component recipes (navy hero, KPI tile, ledger console table, swatch+text status system, button tiers, light/dark-surface forms, empty state, COD highlight, QNAS plate, timeline, PWA touch rules) and 5 hard rules (yellow scarcity, AA contrast, no inline styles). Wired it into both paths: /frontend command now points to it as canonical (also fixed the wrong brand-kit.css filename), frontend-designer's supporting-skills section mandates loading it first, and CLAUDE.md instructs loading it before ANY design skill including the read-only /frontend-design plugin.

**Files:** `.claude/skills/brandkit.md`, `.claude/commands/frontend.md`, `.claude/skills/frontend-designer.md`, `CLAUDE.md`

## 10:52 — Careers page redesign: "hiring manifest" concept
Replaced the templated careers page (centered badge hero, icon job cards, yellow icon-circle benefits) with a brand-locked logistics design: navy hero with mono yellow kicker and a static dashed hiring route (Application → Review 48h → Interview → Offer), roles typeset as waybill ledger rows with mono refs (EZY-HR-01…) including a Delivery Driver row that routes to the dedicated driver flow, a quiet no-icon "What you get" terms grid, and the form restyled as a navy-headed Dispatch Note with the page's single yellow CTA. Apply links prefill the job field via data-job. Entry animations were built then removed after screenshots showed them frozen in headless rendering (and per restraint guidance). Verified desktop + mobile screenshots.

**Files:** `webpages/templates/webpages/careers.html`, `webpages/static/webpages/css/careers.css`

## 11:20 — Brand forms system restyled to enterprise register
Rewrote templates/static/base-forms.css (the centralized form system loaded by all four dashboard bases) from the old candy look (yellow radial glow blob in card headers, gradient glow submit buttons, gradient pill badges, three conflicting focus systems, inputs that translateY on focus) to the brand's dispatch-note register: hairline 1px grey-300 borders, radius-sm, small-caps grey labels, the navy "ledger rule" as card-header signature, navy submit + segmented radio choices with navy checked state, flat semantic bg+text badges/notes with 3px ledger flag, one focus system (yellow border + focus-visible outline), navy checkbox marks, errors unified on --brand-danger. Every selector preserved verbatim — templates untouched; iOS 16px/44px touch/reduced-motion/high-contrast blocks kept. Verified business pickup-location (bcon__ overlay unaffected), product combo form, and workforce seller detail render clean; ?v=20260722a bumped on all four base templates.

**Files:** `templates/static/base-forms.css`, `templates/business_dashboard_base.html`, `templates/wf_dashboard_base.html`, `templates/fleet_dashboard_base.html`, `templates/wh_dashboard_base.html`

## 09:14 — 3PL inquiry page redesigned as consignment-note document
Redesigned /3pl/inquiry/ from the old candy look (yellow glow pill radios, bouncy glowing stepper, shine-sweep buttons, gradient WhatsApp modal header) to a restrained consignment-note register: navy document header strip ("Ezzy Delivery · Doha, Qatar / Form RRQ-3PL"), CSS-counter-numbered waybill boxes replacing icon labels, mono routing-strip stepper, hairline stamp-chip selections that fill with navy ink, and a CSS-drawn barcode edge in the left-aligned hero. Yellow now appears exactly twice (hero kicker + primary CTA) per Brand Kit accent budget; all animations reduced and prefers-reduced-motion respected. Dropped dead CSS blocks (wppi success/hook/hero-cta/social — unused by any template). All JS hooks, IDs, and form behavior preserved; verified live at desktop and 390px mobile via Playwright screenshots.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`, `webpages/static/webpages/css/delivery_pricing_inquiry.css`

## 09:44 — 3PL inquiry: checked-chip contrast fix + mobile touch-first pass
Fixed invisible dark-on-navy text on selected Yes/No chips: brandkit.css forces .form-check-label colors with !important, so the page's checked/hover chip rules now mirror that (!important white on navy), and hover on an active chip/bubble no longer flips text to navy-on-navy. Then a small-screen pass in the ≤576px media query per the MB-optimization approach (desktop untouched): 16px input/select font to stop iOS focus-zoom, ≥44px touch targets everywhere, category bubbles become full-width justified stamp rows, Yes/No radios split the row as half-width tap targets, stepper labels wrap instead of colliding at the viewport edge, and :active tap feedback replaces hover. Verified live at 390px via Playwright screenshots.

**Files:** `webpages/static/webpages/css/delivery_pricing_inquiry.css`, `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 09:49 — 3PL inquiry: stylesheet polish + keyboard access for custom selects
Quality pass on delivery_pricing_inquiry.css: the mono document face is now a single --wppi-mono variable (was seven repeated font stacks), the mobile .container gutter override is scoped to the page's own wrappers instead of every container in the layout, the doc-header strip wraps on very narrow phones, and the JS-injected validation errors got a dedicated style. Bigger win: the custom select dropdowns were div-based and completely keyboard-inaccessible — triggers now get tabindex/role/aria-haspopup on init, with Enter/Space toggle, ArrowUp/Down option navigation (kb-focus highlight), and Escape to close, plus a visible navy focus-visible outline. Verified live; markers present in served HTML.

**Files:** `webpages/static/webpages/css/delivery_pricing_inquiry.css`, `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 09:54 — brandkit.css design audit: contrast bugs, undefined token, legacy yellow drift
Design pass on the sitewide brandkit.css (loaded by all five base templates). Fixed: (1) --brand-grey-900 was referenced by 10+ page stylesheets but never defined — every such color computed to `unset`; now defined as #111111. (2) .nav-tabs .nav-link.active had background var(--brand-grey) #333 with grey-800 text — invisible active tabs; now white per the tab's own border design. (3) Removed !important from the three global .form-check-label rules — the foot-gun that broke the 3PL inquiry chips; page designs can now restyle labels normally. (4) Unified legacy yellow #ffd33b / rgba(255,211,59,…) to the true brand #f7c000 everywhere (--brand-yellow alias, focus rings, --bs-primary/--bs-warning/--bs-link-color). (5) .text-success/.text-danger/.text-warning/.text-info and stat-card deltas moved off neon hexes (#38ef7d was 1.9:1 on white) onto the AA semantic tokens. (6) Added the missing h4 to the heading scale. Smoke-tested /, /3pl/inquiry/, /delivery-doha/ (all 200) + homepage screenshot clean.

**Files:** `webpages/static/webpages/css/brandkit.css`, `templates/includes/head.html`

## 10:34 — 3PL inquiry form: 8 new pricing-relevant fields
Added the agreed missing fields to the PricingEnquiry model + form (migration 0007): COD share of orders (conditional on COD=Yes), storage volume (conditional on Doha hub=Yes), current cost per delivery (optional select), special-handling detail multi-select (conditional on special handling=Yes), average package weight, number of pickup locations (conditional on Multiple Store), free-text additional notes, and a required contact-consent checkbox validated at submit. Conditionals follow the existing show/hide pattern (server-rendered style + JS change listeners; bubble-driven toggle for pickup type). Step 2/3 save helpers extended; staff preview page shows all new fields plus a Client Notes panel; admin picks them up automatically. Migrated production DB, reloaded, and verified all 10 conditional-toggle behaviors live via Playwright (clicks only, nothing submitted).

**Files:** `webpages/models.py`, `webpages/views.py`, `webpages/migrations/0007_pricingenquiry_additional_notes_and_more.py`, `webpages/templates/webpages/delivery_pricing_inquiry.html`, `webpages/templates/webpages/inquiry_preview.html`

## 10:40 — 3PL inquiry: optional email field
Added an optional email field to the inquiry (migration 0008): EmailField on PricingEnquiry, step-1 form input ("we'll send your quote here") with a format check in JS only when non-empty, saved via the step-1 helper, shown as a mailto item on the staff preview contact panel, and included in the WhatsApp admin notification line-up. Migrated production DB, reloaded, verified render.

**Files:** `webpages/models.py`, `webpages/views.py`, `webpages/templates/webpages/delivery_pricing_inquiry.html`, `webpages/templates/webpages/inquiry_preview.html`, `core/whatsapp_utils.py`, `webpages/migrations/0008_pricingenquiry_email.py`

## 10:42 — 3PL inquiry: order-history questions adapt to new vs existing business
Order Volumes (step 2) now keys off the step-1 "How long have you been operating?" answer instead of asking twice: when it's "New (not started yet)", the Orders Last Week / Orders Last Month questions hide (server-rendered initial state + live JS toggle on the custom select), a "no order history needed" hint shows, any previously picked history answers are cleared, and step-2 validation stops requiring them — the form then quotes purely on expected volumes. Existing businesses see the full section unchanged. Verified live with 7 browser assertions (toggle both directions + stale-value clearing).

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 10:47 — 3PL inquiry: explicit new/existing toggle in Order Volumes
Replaced the silent step-1-derived behavior with a visible question at the top of Order Volumes: "Is your business already operating?" (Yes — already taking orders / No — new business). Picking "No" hides Orders Last Week/Last Month, shows the no-history hint, clears any stale answers, and drops those fields from validation. The toggle defaults from the step-1 operating-age answer (server-derived via _derive_business_status, live-synced when the age select changes) and persists in the session across steps; it's a UI control only, not a DB column. Verified live with 9 browser assertions.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`, `webpages/views.py`

## 10:49 — 3PL inquiry: merged duplicate pickup-time questions
Step 3 asked pickup time twice ("Pickup Time Slot" in Pickup Configuration and "Preferred Pickup Time" in Systems & Communication, near-identical options). Kept the Pickup Configuration one relabeled "Preferred Pickup Time" (input name pickup_location_time_slab unchanged) and removed the duplicate block. No columns dropped: the step-3 save helper mirrors the single answer into preferred_pickup_time too, so the workforce detail page, editable-fields list, and old records keep working; the staff preview now shows one row that falls back across both columns for legacy data. Verified live: only one pickup-time input renders, step 3 loads 200.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`, `webpages/views.py`, `webpages/templates/webpages/inquiry_preview.html`

## 10:50 — 3PL inquiry: delivery speed options updated
Reworked the "Delivery Speed Offered to Customers" chips on step 3: removed "Next Day" and "2–3 Days", added "48 Hours", and reordered fastest-first — Express (Few Hours), Same Day, 48 Hours, Standard (3–5 Days). Old records with removed values are unaffected (free-text column). Verified live.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 10:52 — 3PL inquiry: package size + weight side by side
Moved Average Package Weight next to Typical Package Size as a col-md-6 pair on the same row in the Delivery Preferences box, and widened Preferred Delivery Time Window to full width so the grid pairs cleanly. Old duplicate weight block removed (single id verified). Reloaded and confirmed live.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 10:54 — 3PL inquiry: OMS question now required
"How do you manage orders?" (order_management_system, step 3) is now mandatory: required asterisk on the label and a validateStep(3) check that blocks submit with an inline error until a chip is selected.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 10:56 — 3PL inquiry: package-size chips get icons + descriptors
Answered "add sample pics?" with the restrained version: no photos, but each Typical Package Size chip now carries a small FA icon and a one-word descriptor — envelope "Small · Envelopes", box "Medium · Shoe box", stacked boxes "Large · Bulky", layers "Mixed". Icons inherit chip ink (white when selected). Verified live with a screenshot; size and weight confirmed side by side.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 10:58 — 3PL inquiry: package-size icons now colored
Per feedback that the size icons read as monochrome, each Typical Package Size chip icon now has its own semantic-palette color (envelope blue, box amber, stacked-boxes green, layers grey) via nth-child rules scoped to the size group, flipping to white on the selected navy chip. Cache-buster bumped to ?v=20260722f, collectstatic + reload, verified with screenshot.

**Files:** `webpages/static/webpages/css/delivery_pricing_inquiry.css`, `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 11:02 — 3PL inquiry: colored icons rolled out to all meaningful chip questions
Extended the icon treatment across the form with a reusable utility set (.wppi__ico--info/warn/success/danger/purple/grey/navy/wa in delivery_pricing_inquiry.css, replacing the packagesize nth-child rules; selected chips force white icons). Icons added to: product categories (12 chips), delivery speed, delivery time window, package weight ladder (feather→dumbbell), special-handling detail, pickup location type, pickup time, order management (brand icons: Shopify/WordPress/Instagram), communication channel (WhatsApp green), free delivery, and payment method. Numeric range questions (volumes, COD share, counts, cost, storage) deliberately left icon-free. ?v=20260722g, collectstatic + reload, verified with step-1 and step-3 screenshots.

**Files:** `webpages/templates/webpages/delivery_pricing_inquiry.html`, `webpages/static/webpages/css/delivery_pricing_inquiry.css`

## 11:10 — 3PL inquiry: dropdowns no longer clipped at card edge
The custom select options ("Current Cost Per Delivery", "When do you want to start?" — last group in step 2) were being cut off because .wppi__form-card had overflow:hidden for its rounded corners. Removed the overflow and gave the navy doc-header strip its own top radius instead; open dropdowns now extend cleanly past the card edge (verified live: all 5 timeline options visible and clickable, screenshot confirms). ?v=20260722h.

**Files:** `webpages/static/webpages/css/delivery_pricing_inquiry.css`, `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 11:26 — 3PL inquiry: form wrapper horizontal padding removed
#inquiryFormWrap now has zero left/right padding at all breakpoints (the card supplies its own edges), and the old 0.875rem mobile gutter override for the wrapper was dropped; the hero container keeps its mobile gutters. Verified computed 0px at 1440px and 390px. ?v=20260722i.

**Files:** `webpages/static/webpages/css/delivery_pricing_inquiry.css`, `webpages/templates/webpages/delivery_pricing_inquiry.html`

## 11:31 — 3PL pricing: Zella partner cards link to matching services
Each of the four Zella Qatar "Digital Partner" cards on /3pl/pricing/ now has a second "View Service" button (navy, new tab) deep-linking to the matching section on zellaqatar.com/services/. Zella's page has no anchor ids and its codebase is read-only from this account, so the links use text fragments (#:~:text=...) that scroll-and-highlight the section in modern browsers and fall back to the page top elsewhere: Automation → "Automation (Auto Replies)", Shopify Store Build → "Shopify Build", Performance Marketing → "Performance Marketing", Store Management → "Shopify Management". WhatsApp CTAs unchanged. Verified 4 links render live.

**Files:** `webpages/templates/webpages/3pl_pricing.html`

## 18:00 — Fix missing accuracy chip on workforce orders list for by_customer/by_staff coords
The eden (EDN132) orders showed no location-accuracy chip on /workforce/orders/all/ because their coords_accuracy is 'by_customer' / 'by_staff' — newer choices the orders list template's if/elif chain never handled, so the chip rendered empty. Added Client/Staff/Driver labels to both chip spots in the list, added the missing 'by_driver' case to the order detail page, and added orc__/odp__ color styles for the three source-based values. Cache-buster bumped, static collected, gunicorn reloaded, site HTTP 200.

**Files:** `workforce/templates/workforce/parts/lists/orders_list_view.html`, `workforce/templates/workforce/order_detail.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`
