# Dev Log — 2026-07-21

## 15:45 — Searchable lead dropdown in Link CRM Lead modal
Replaced the plain `<select>` in the Business Verification "Link CRM Lead" modal with a searchable combobox: a text input filters open leads by name or phone (all phone variants included in the match), with keyboard navigation (arrows/Enter/Escape) and a capped 50-result dropdown list. The original select stays hidden as the value store, so the phone-based auto-preselect and the link POST flow are unchanged. Picker styles added to workforce.css under the `uvl__lead-picker` block; cache-buster bumped.

**Files:** `workforce/templates/workforce/business_verification_list.html`, `workforce/static/workforce/css/workforce.css`, `templates/wf_dashboard_base.html`

## 17:54 — Pickup location QNAS auto-pin + staff edit
Pickup locations saved without lat/lon now auto-geocode via QNAS (zone+street+building → exact building pin, falling back to street/zone-center) through a new pre_save signal, matching the geocoder orders already use. Added a staff edit endpoint (wf_pickup_location_update) and wired an edit button into the seller detail page's pickup location modal (add/edit dual mode, reload on save). Backfilled the 2 existing pinless locations — Rowaa Studio (Zone 46/St 865/Bldg 15) and mini snug "Main branch" — both resolved to exact building coordinates.

**Files:** `business/signals.py`, `workforce/views.py`, `workforce/urls.py`, `workforce/templates/workforce/seller_detail.html`
